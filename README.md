# med-de-id

Windows-first, local-first medical de-identification system with a pure Rust core.

## What it is

`med-de-id` is a governed workflow system for reversible medical data de-identification. It is designed for local/offline use, keeps sensitive assets on-device by default, and supports controlled decode/re-identification with audit trails.

The product has three formal surfaces:

1. **CLI** — automation, batch, integration, headless execution
2. **Browser tool** — local-first browser surface with bounded localhost workflows for tabular de-identification, PDF review, DICOM helper output, conservative media metadata review, vault audit/decode/export, and portable artifact inspect/import
3. **Desktop app** — sensitive workstation for review, vault operations, decode flows, and audit investigation

## Core workflow

```text
ingest -> extract -> detect -> review -> encode -> export -> decode -> audit
```

## Design principles

- **Pure Rust core**
- **Windows-first**
- **Local-first / offline-capable**
- **Reversible mapping stored in a protected vault**
- **High-risk decode is explicit and auditable**
- **Broad format-family coverage with honest depth grading**
- **Tri-surface product model with layered responsibility**

## Planned format support

| Format family | v1 depth | Notes |
|---|---|---|
| DICOM | L3 | tag-level handling, UID remap, private-tag policy, burned-in suspicion flagging |
| CSV | L3 | schema-aware reversible mapping and batch consistency |
| Excel (XLSX, bounded) | L2 | schema-aware reversible mapping on only the first non-empty worksheet; runtime XLSX output is bounded to that route's rewritten workbook behavior, while CLI XLSX output is a normalized single-sheet workbook generated from de-identified tabular rows and does not preserve original workbook metadata, formatting, formulas, sheet names, or multiple sheets |
| PDF / scanned records | L1/L2 foundation | text-layer extraction, OCR-needed suspicion routing, mixed multi-page summary/reporting, invalid-PDF rejection as parse failure, and bounded clean text-layer PDF byte export/download when no review/OCR/visual blockers remain; no full OCR, visual redaction, handwriting handling, scanned-PDF rewrite, PDF page pixel redaction, or full rewrite/export for candidate-bearing/blocked PDFs yet |
| FCS | L2/L3 metadata-first | TEXT/metadata identifier handling |
| Images | L1/L2 bounded foundation | filename/path/metadata cleanup and metadata-only suspicion routing, plus bounded PPM P6 byte export for explicit bbox redaction regions with PHI-safe aggregate visual verification metadata and a runtime/application `POST /visual-redaction/ppm` JSON surface for PPM-only bbox redaction; no OCR, PNG/JPEG rewrite, PDF/video export, browser/desktop visual execution, or full multi-format visual verification yet |
| Videos | L1 | filename/path/container metadata and sidecar handling |

## Architecture overview

```text
shared Rust workspace today
├─ mdid-domain
├─ mdid-vault
├─ mdid-adapters
├─ mdid-application
├─ mdid-runtime
├─ mdid-cli
├─ mdid-browser
└─ mdid-desktop
```

Planned follow-on core crates from the design, not yet implemented in this repository:

- `mdid-policy`
- `mdid-detection`

## Current repository status

Completion snapshot, based only on landed repository features and repository-visible verification state (truth-synced 2026-05-02 through the bounded Browser/Web PPM P6 visual redaction mode after the bounded clean text-layer PDF export download, bounded PPM runtime/CLI visual redaction evidence, bounded CLI/runtime text-only Privacy Filter DRIVER_LICENSE/VIN/DEA/FAX detection slices, and the CLI 99%-impact batch). This round adds Browser/Web execution for explicit bbox-driven PPM P6 visual redaction using the existing local runtime, while the displayed Browser/Web score remains at the already-reached 99% target cap; external real-model quality remains described separately and is not overclaimed. It does not claim Desktop execution, cloud/SaaS processing, OCR, automatic visual detection, PNG/JPEG/PDF/video rewrite, handwriting recognition, or final PDF rewrite/export for blocked/candidate-bearing PDFs.

| Area | Completion | Status |
|---|---:|---|
| CLI | 99% | CLI automation now includes bounded text-only Privacy Filter coverage for DATE/DOB, ADDRESS, ZIP, SSN, PASSPORT, `INSURANCE_ID`, `DEA_NUMBER`, `VIN`, `AGE`, `FACILITY`, `NPI`, `LICENSE_PLATE`, `DRIVER_LICENSE`, `IP_ADDRESS`, `URL`, context-required `FAX`, and phone numbers with extension formats; local OCR/Privacy Filter readiness evidence; metadata-only conservative media export evidence; encrypted portable transfer summary; and offline-readiness packaging/governance evidence outputs. This round adds one new CLI/runtime text-PII detector requirement and completes it in the same slice (`136/141 -> 137/142 = 96%` floor), so the displayed CLI score remains at the already-reached 99% target cap for local-first automation evidence. Remaining non-goals are external real OPF/PP-OCR model-quality benchmarking and full cross-surface UX polish, not CLI automation blockers. |
| Browser/Web | 99% | Browser/Web remains at the 99% target cap from previously landed PHI-safe summary/download helpers and bounded localhost workflows. This round adds bounded Browser/Web execution for explicit bbox-driven PPM P6 visual redaction through the existing local `/visual-redaction/ppm` runtime endpoint and a gated redacted PPM download, but does not raise the already capped score. Browser/Web +5% rule: FAIL/not claimed because the surface is already capped at the 99% target. |
| Desktop app | 99% | Desktop app remains at the 99% target cap from previously landed PHI-safe summary save helpers and bounded workstation workflows. This CLI-only round does not add Desktop execution. Desktop +5% rule: FAIL/not claimed because the surface is already capped at the 99% target. |
| Overall | 99% | Overall reaches the 99% target cap for repository-bounded local-first de-identification evidence because CLI, Browser/Web, and Desktop are each at 99%. The remaining 1% is intentionally reserved for external real-model quality benchmarking, distribution packaging outside this repo, and field validation. |


Verification evidence for the `mdid-cli privacy-filter-text` FAX detection slice landed on this branch: the bounded local text-only Privacy Filter runner now detects explicitly contextual fax numbers such as `fax 555-222-3333`, `fax: (555) 444-5555`, and `fax 555-123-4567 x890` as `FAX`, masks them as `[FAX]`, emits only `<redacted>` span previews, and keeps `metadata.network_api_called: false`. The detector requires fax/facsimile context, does not classify plain phone numbers or overlong fax-like values as `FAX`, and masks bounded fax extensions as part of the FAX span so validator output remains non-overlapping. Rust CLI validation now accepts `FAX`, and stdin smoke coverage proves stdout, stderr, and report contents do not leak the raw fax number, synthetic patient name/MRN, report path, or PHI-bearing temp directory. Completion accounting adds the new requirement to both denominator and numerator (`133/138 -> 134/139 = 96%` floor), leaving displayed completion at CLI 99%, Browser/Web 99%, Desktop app 99%, and Overall 99%; Browser/Web +5% and Desktop +5% are FAIL/not claimed because this is CLI/runtime text-only Privacy Filter evidence only, not Browser/Web or Desktop capability. It is not OCR, visual redaction, image pixel redaction, final PDF rewrite/export, or real-model quality proof.

Verification evidence for the `mdid-cli privacy-filter-text` DEA registration number detection slice landed on this branch: the bounded local text-only Privacy Filter runner now detects explicitly contextual and checksum-valid DEA registration numbers such as `DEA AB1234563` as `DEA_NUMBER`, masks only the raw token as `[DEA_NUMBER]`, emits only `<redacted>` span previews, and keeps `metadata.network_api_called: false`. The detector requires explicit DEA context and rejects uncontextual, invalid-checksum, embedded/unbounded, MRN-prefixed, and ID-prefixed DEA-like tokens. Rust CLI validation now accepts `DEA_NUMBER`, and stdin smoke coverage proves stdout, stderr, and report contents do not leak the raw DEA number, synthetic patient name/MRN, report path, or PHI-bearing temp directory. Completion accounting adds the new requirement to both denominator and numerator (`134/139 -> 135/140 = 96%` floor), leaving displayed completion at CLI 99%, Browser/Web 99%, Desktop app 99%, and Overall 99%; Browser/Web +5% and Desktop +5% are FAIL/not claimed because this is CLI/runtime text-only Privacy Filter evidence only, not Browser/Web or Desktop capability. It is not OCR, visual redaction, image pixel redaction, final PDF rewrite/export, or real-model quality proof.

Verification evidence for the `mdid-cli privacy-filter-text` VIN detection slice landed on this branch: the bounded local text-only Privacy Filter runner now detects contextual VINs such as `vehicle VIN 1HGCM82633A004352` as `VIN`, masks the raw token as `[VIN]`, emits only `<redacted>` span previews, and keeps `metadata.network_api_called: false`. The detector rejects uncontextual, embedded/unbounded, MRN-context, and forbidden I/O/Q VIN alphabet tokens. The validator now accepts `VIN`. Repository-visible verification passed with the targeted VIN Python tests and the full `python3 scripts/privacy_filter/test_run_privacy_filter.py -v` suite with 48 tests. Completion accounting adds one requirement to both denominator and numerator (`135/140 -> 136/141 = 96%` floor), leaving displayed completion at CLI 99%, Browser/Web 99%, Desktop app 99%, and Overall 99%; Browser/Web +5% and Desktop +5% are FAIL/not claimed because this is CLI/runtime text-only Privacy Filter evidence only and those surfaces are already capped at 99%. It is not OCR, visual redaction, image pixel redaction, handwriting recognition, final PDF rewrite/export, or model-quality proof.

Verification evidence for the `mdid-cli privacy-filter-text` driver license detection slice landed on this branch: the bounded local text-only Privacy Filter runner now detects explicitly contextual driver-license identifiers such as `driver license D1234567` as `DRIVER_LICENSE`, masks the raw token as `[DRIVER_LICENSE]`, emits only `<redacted>` span previews, and keeps `metadata.network_api_called: false`. The detector rejects standalone, embedded/unbounded, MRN-context, ID-context, generic non-driver license contexts, and non-identifier license-like tokens. The validator and Rust CLI validation now accept `DRIVER_LICENSE`. Repository-visible verification passed with targeted driver-license Python tests, the full `python3 scripts/privacy_filter/test_run_privacy_filter.py -v` suite, the targeted `cargo test -p mdid-cli cli_privacy_filter_text_detects_driver_license_from_stdin_without_raw_value_leaks --test cli_smoke -- --nocapture` smoke test, and the `cargo test -p mdid-cli privacy_filter_text --test cli_smoke -- --nocapture` regression suite. Completion accounting adds one requirement to both denominator and numerator (`136/141 -> 137/142 = 96%` floor), leaving displayed completion at CLI 99%, Browser/Web 99%, Desktop app 99%, and Overall 99%; Browser/Web +5 and Desktop +5 are FAIL/not claimed because this is CLI/runtime text-only Privacy Filter evidence and those surfaces are already capped at 99%. It is not OCR, visual redaction, image pixel redaction, handwriting recognition, final PDF rewrite/export, Browser/Web execution, Desktop execution, or model-quality proof.

Verification evidence for the CLI 99%-impact batch landed on this branch: `mdid-cli privacy-filter-text` and the runtime `/privacy-filter/text` summary now count bounded phone extension formats without leaking raw phone values; `offline-readiness` accepts optional `--packaging-output` and `--governance-output` artifacts and writes PHI-safe aggregate JSON that omits raw paths and values. Repository-visible verification includes targeted offline-readiness CLI/unit smoke, privacy-filter phone-extension Python tests, runtime phone-extension test, full `cargo test -p mdid-cli`, formatting, and diff checks.

Verification evidence for the `run_ocr_batch_quality.py` OCR batch quality runner slice landed on this branch: the bounded local PP-OCRv5 mobile printed-text extraction path now has a multi-file aggregate runner that invokes the existing single-image OCR JSON runner per input, continues after per-file failures, emits PHI-safe aggregate counts and item statuses, and separates fixture/mixed execution evidence from complete local-real-adapter aggregate text metrics. The local environment has now installed PaddleOCR/PaddlePaddle and downloaded cached PaddleX PP-OCRv5 official model assets under `~/.paddlex/official_models`; a real non-mock probe executed successfully with `engine_status: local_paddleocr_execution`, but the synthetic fixture OCR quality was poor (`normalized_text: "KSSS"`), so this remains real-runtime readiness evidence only, not model-quality acceptance. Prior repository-visible batch-runner verification passed: `python3 -m pytest tests/test_ocr_runner_contract.py -q -k batch`, `python3 -m pytest tests/test_ocr_runner_contract.py tests/test_ocr_handoff_contract.py tests/test_ocr_privacy_evidence_runner.py -q`, SDD spec review PASS, and SDD quality review APPROVED after a partial-failure real-adapter truthfulness fix. This advances the full OCR pipeline backlog only at the CLI/runtime batch-quality evidence layer: it is still not a full OCR pipeline, not external real-model quality benchmarking, not visual redaction, not image pixel redaction, not handwriting recognition, not final PDF rewrite/export, not Browser/Web execution, not Desktop execution, and not workflow orchestration semantics. Completion remains CLI 99%, Browser/Web 99%, Desktop app 99%, Overall 99%; this round adds no completion re-baseline.

Verification evidence for the bounded image pixel redaction foundation landed on this branch: `mdid-domain` now exposes a validated `ImageRedactionRegion` bbox model with constructor and serde-deserialization validation for non-empty regions, and `mdid-adapters` now exposes a pure in-memory RGB redaction helper that blacks or fill-colors approved bbox regions while leaving outside pixels unchanged. Repository-visible verification passed: `cargo test -p mdid-domain --test visual_redaction_models -p mdid-adapters --test image_redaction_adapter`, broader `cargo test -p mdid-domain -p mdid-adapters`, `cargo clippy -p mdid-domain -p mdid-adapters --all-targets -- -D warnings`, `git diff --check`, SDD spec review PASS, and SDD quality review APPROVED after fixing the serde invariant bypass. This advances actual visual/image pixel redaction only at the bounded domain/adapter byte-rewrite foundation layer: it is not automatic visual detection, not face detection, not PDF page redaction, not final PDF rewrite/export, not media-byte rewrite/export beyond RGB buffers, not handwriting recognition, not Browser/Web execution, and not Desktop execution. Completion remains CLI 99%, Browser/Web 99%, Desktop app 99%, Overall 99%; no completion re-baseline is claimed.

Verification evidence for the bounded PPM P6 image byte export slice landed on this branch: `mdid-adapters` now parses bounded PPM P6 RGB bytes with maxval 255, rewrites only explicit approved bbox regions to black, preserves unchanged image bytes, and fails closed on out-of-bounds regions without raw source-name leaks. `mdid-cli redact-image-ppm` reads a local PPM input, accepts explicit `ImageRedactionRegion` JSON, writes the redacted PPM output, and writes a PHI-safe `image_redaction_export_summary` with aggregate counts and byte length only. Repository-visible verification passed: `cargo test -p mdid-cli --test cli_smoke redact_image_ppm -- --nocapture && cargo test -p mdid-adapters --test image_redaction_adapter -- --nocapture`. This evidence is limited to bounded PPM P6 byte export for explicit bbox regions and does not claim PNG/JPEG/PDF/video export or full visual verification.

Verification evidence for the bounded PPM P6 application/runtime visual redaction surface landed on this branch: `mdid-application` now exposes a `VisualRedactionService` that accepts PPM P6 bytes plus explicit `ImageRedactionRegion` bbox regions, delegates to the existing adapter PPM rewrite+verification path, returns rewritten PPM bytes with aggregate PHI-safe verification, redacts debug byte output, and maps malformed PPM/out-of-bounds failures without raw source names or paths. `mdid-runtime` now exposes `POST /visual-redaction/ppm` with JSON `{ "ppm_bytes_base64": "...", "regions": [{ "x": 0, "y": 0, "width": 1, "height": 1 }] }`, returning `{ "rewritten_ppm_bytes_base64": "...", "verification": ... }` on success and `422 invalid_visual_redaction` for malformed base64, malformed PPM, invalid/empty regions, or out-of-bounds regions. This is PPM-only de-identification workflow plumbing and does not claim OCR, automatic visual detection, PNG/JPEG/PDF/media rewrite, browser/desktop execution, or packaging completion. Repository-visible verification run: `cargo test -p mdid-application --test visual_redaction -- --nocapture`; `CARGO_BUILD_JOBS=1 cargo test -p mdid-runtime visual_redaction -- --nocapture`.

Verification evidence for the bounded Browser/Web PPM P6 visual redaction mode landed on this branch: Browser/Web now accepts local `.ppm` imports as base64/data-url payloads, submits explicit operator-provided bbox regions to the existing local `/visual-redaction/ppm` runtime endpoint using the runtime contract `{ "ppm_bytes_base64": "...", "regions": [...] }`, renders only PHI-safe aggregate verification fields in the visible UI, and exposes a binary `mdid-browser-redacted.ppm` download only when the runtime returns verified `ppm_p6` redaction bytes with changed pixels inside approved regions. Repository-visible verification passed: `cargo test -p mdid-browser visual_redaction_ppm -- --nocapture`, `cargo test -p mdid-browser --lib -- --nocapture`, `cargo test -p mdid-browser pdf_clean_text_layer_export -- --nocapture`, `cargo fmt --check`, `git diff --check`, SDD spec review PASS, and SDD quality review APPROVED after runtime-contract and display-sanitization fixes. This advances Browser/Web workflow depth and actual visual/image pixel redaction only for bounded PPM P6 explicit bbox byte rewrite/download; it does not claim OCR, automatic visual detection, handwriting recognition, PNG/JPEG rewrite, PDF page redaction/rewrite, video/FCS media-byte export, Desktop execution, packaging, or field validation.

Verification evidence for the bounded Browser/Web clean text-layer PDF export download slice landed on this branch: Browser/Web PDF mode now keeps existing PDF review-report downloads separate while adding a gated clean text-layer PDF export evidence download when the runtime response contains an empty review queue, `no_rewritten_pdf: false`, `review_only: false`, and non-empty `rewritten_pdf_bytes_base64`. The browser preserves clean runtime JSON internally only for sanitized export, withholds PDF runtime JSON from visible UI to avoid PHI exposure, sanitizes PDF page-status/review-queue display so source labels and raw `source_text` are not rendered, and disables the clean export button for review-only, no-rewrite, missing-byte, null-byte, non-JSON, or review-queue-blocked responses. Repository-visible verification passed: `cargo test -p mdid-browser pdf_clean_text_layer_export -- --nocapture`, `cargo test -p mdid-browser pdf_ -- --nocapture`, SDD spec review PASS, SDD quality review APPROVED, `cargo fmt --check`, and `git diff --check`. This advances Browser/Web workflow depth for bounded clean text-layer PDF export/download evidence only; it does not claim OCR, visual redaction, handwriting handling, scanned-PDF rewrite, PDF page pixel redaction, full PDF rewrite/export for candidate-bearing PDFs, Desktop execution, packaging, or field validation.

Verification evidence for the bounded PPM P6 visual verification loop landed on this branch: `mdid-adapters` now returns a PHI-safe `PpmRedactionVerification` artifact for redacted PPM exports with `format`, dimensions, redacted region count, distinct redacted pixel count, unchanged pixel count, output byte count, and a truthfully computed `verified_changed_pixels_within_regions` flag that compares original and output pixels against the approved redaction mask/fill. Overlapping bbox regions count distinct pixels once, out-of-bounds regions fail closed without a verification artifact, and `mdid-cli redact-image-ppm` includes the same verification object under `visual_verification` in its PHI-safe summary without source paths, filenames, bbox arrays, raw PPM headers, or image bytes. Repository-visible verification passed: `cargo test -p mdid-adapters --test image_redaction_adapter ppm -- --nocapture`, `cargo test -p mdid-cli --test cli_smoke redact_image_ppm -- --nocapture`, SDD spec review PASS, SDD quality review APPROVED, and `cargo clippy -p mdid-adapters -p mdid-cli --tests -- -D warnings` in quality review. This advances actual visual/image pixel redaction only for bounded PPM P6 bbox-driven byte export verification; it does not claim OCR, handwriting recognition, PNG/JPEG rewrite, PDF page redaction/rewrite, video/FCS media-byte export, Browser/Web execution, Desktop execution, packaging, or field validation.

Verification evidence for the `run_small_ocr.py --json` PHI-safe text-metrics slice landed on this branch: the bounded PP-OCRv5 mobile small OCR JSON contract now emits aggregate-only `line_count` and `normalized_char_count` fields derived from printed-text extraction output, while preserving redacted source metadata and downstream `text_only_normalized_input` readiness. Repository-visible verification passed: `python3 -m pytest tests/test_ocr_runner_contract.py -q`, the broader OCR contract suite (`tests/test_ocr_runner_contract.py`, `tests/test_ocr_handoff_contract.py`, `tests/test_ocr_privacy_evidence_runner.py`, `tests/test_ocr_handoff_corpus.py`, `tests/test_ocr_to_privacy_filter_corpus.py`, and `tests/test_ocr_handoff_builder_failures.py`) with 36 tests passing, SDD spec review PASS, and SDD quality review APPROVED. This is CLI/runtime PP-OCRv5 mobile printed-text extraction readiness evidence only: not OCR model-quality proof, not visual redaction, not image pixel redaction, not handwriting recognition, not final PDF rewrite/export, not Browser/Web execution, not Desktop execution, and not workflow orchestration semantics. Completion remains CLI 99%, Browser/Web 99%, Desktop app 99%, Overall 99%; this round adds no new rubric item beyond the existing OCR readiness/handoff contract, so there is no completion re-baseline.

Verification evidence for the CLI completion-impact batch landed on this branch: `review-media --export-report` now writes a PHI-safe conservative-media metadata-only export evidence artifact with `rewrite_status: metadata_only_export`, `rewrite_available: false`, `raw_source_included: false`, and `rewritten_media_bytes: null`, while preserving explicit non-goals for media-byte rewrite, OCR, and visual redaction. `portable-transfer-summary` now unlocks an encrypted portable artifact and prints a PHI-safe workflow summary with record count and next-step guidance without exposing raw values or artifact paths. This reduces the CLI/product-core blockers around conservative media export evidence and generalized portable transfer workflow UX without claiming full media-byte rewrite/export, visual redaction, Browser/Web execution, Desktop execution, packaging completion, or full policy/detection completion. Repository-visible verification passed: targeted conservative media export smoke, portable transfer parser test, and `cargo test -p mdid-cli` with 41 unit tests, 2 CLI PDF tests, and 125 CLI smoke tests.

Verification evidence for the `mdid-cli privacy-filter-text` IP address detection slice landed on this branch: the bounded local text-only Privacy Filter runner now detects syntactically valid IPv4 addresses such as `192.168.10.42` as `IP_ADDRESS`, masks them as `[IP_ADDRESS]`, emits only `<redacted>` span previews, and keeps `metadata.network_api_called: false`. The detector rejects invalid or embedded IPv4-like tokens such as `999.168.10.42`, `host192.168.10.42`, and `192.168.10.42extra`. The Rust CLI summary/category validation now accepts `IP_ADDRESS`, and stdin smoke coverage proves stdout, stderr, and report contents do not leak the raw IP address, synthetic names, MRNs, report paths, or temp directories. Repository-visible verification passed: `python -m pytest scripts/privacy_filter/test_run_privacy_filter.py -q`, `python scripts/privacy_filter/validate_privacy_filter_output.py /tmp/privacy-filter-ip-address.json`, and `cargo test -p mdid-cli privacy_filter_text --test cli_smoke -- --nocapture`.

Verification evidence for the `mdid-cli privacy-filter-text` URL detection slice landed on this branch: the bounded local text-only Privacy Filter runner now detects explicit `http://` and `https://` URL tokens with dotted hosts and 2-8 letter TLDs as `URL`, masks them as `[URL]`, emits only `<redacted>` span previews, and keeps `metadata.network_api_called: false`. The detector rejects non-http schemes, no-dot hosts, embedded prefixes, and long-TLD synthetic tokens such as `https://portal.example.testextra/path`. The Rust CLI summary/category validation now accepts `URL`, and stdin smoke coverage proves stdout, stderr, and report contents do not leak the raw URL, synthetic names, MRNs, report paths, or temp directories. Fraction accounting for this URL requirement adds one completed CLI/runtime text-PII detector item on top of the prior URL-free baseline (`132/137 -> 133/138 = 96%` floor); Browser/Web remains 99%, Desktop app remains 99%, and Browser/Web +5 / Desktop +5 are FAIL/not claimed because no user-facing Browser/Web or Desktop capability landed.

Verification evidence for the `mdid-cli privacy-filter-text` five-gap closure batch landed on this branch: the bounded local text-only Privacy Filter runner now detects explicit insurance/member/policy-context synthetic identifiers as `INSURANCE_ID`, age phrases as `AGE`, provider/facility phrases as `FACILITY`, checksum-valid NPI values as `NPI`, and license-plate-context values as `LICENSE_PLATE`; masks them with bracketed category tokens; emits only `<redacted>` span previews; and keeps the existing local/no-network Privacy Filter contract. The detectors are bounded against standalone/embedded identifier-like tokens, out-of-range ages, no-context facility names, invalid NPI values, and no-context plate-like tokens. Rust CLI summary/category validation accepts all five labels, and stdin smoke coverage proves stdout, stderr, and report contents do not leak raw synthetic identifiers, synthetic name, report path, or temp directory. Repository-visible verification for this final truth-sync: `python3 -m pytest scripts/privacy_filter/test_run_privacy_filter.py -q` passed with 34 tests, `cargo test -p mdid-cli privacy_filter --test cli_smoke` passed with 59 tests, `git diff --check` passed, and `cargo fmt --all -- --check` passed. This is CLI/runtime text-only PII detection coverage: not OCR, not visual redaction, not image pixel redaction, not handwriting recognition, not final PDF rewrite/export, not Browser/Web execution, not Desktop execution, and not workflow orchestration. Fraction accounting starts from the prior CLI `126/131 = 96%` floor and adds/completes five CLI/runtime text-PII requirements in this round: CLI `131/136 = 96%` floor, Browser/Web remains 99%, Desktop app remains 99%, and Overall remains 97%, with Browser/Web +5 and Desktop +5 FAIL/not claimed because no Browser/Web or Desktop surface capability landed and both are already at the 99% target cap.

Verification evidence for the `mdid-cli privacy-filter-text` PASSPORT detection slice landed on this branch: the bounded local text-only Privacy Filter runner now detects standalone synthetic passport-like identifiers such as `X12345678` and explicit passport-context numeric identifiers such as `passport 123456789` as `PASSPORT`, masks them as `[PASSPORT]`, emits only `<redacted>` span previews, and keeps `metadata.network_api_called: false`. The detector is bounded against adjacent alphanumeric/hyphen characters and MRN/ID overlaps; regression coverage proves `MRN-X12345678`, `ID X12345678`, `MRN-123456789`, `ID-123456789`, `passport 123456789-01`, embedded tokens, overlong tokens, and address-like numeric text are not overbroad PASSPORT detections, while alphanumeric MRN/ID variants are masked as MRN/ID rather than left raw. The Rust CLI summary/category validation now accepts `PASSPORT`, and stdin smoke coverage proves stdout, stderr, and report contents do not leak the raw passport value, synthetic name, MRN, report path, or temp directory. Repository-visible verification: `python -m pytest scripts/privacy_filter/test_run_privacy_filter.py -q`, `cargo test -p mdid-cli cli_privacy_filter_text_detects_passport_without_phi_leaks --test cli_smoke -- --nocapture`, `cargo test -p mdid-cli privacy_filter_text --test cli_smoke -- --nocapture`, `cargo fmt --check`, and `git diff --check` passed during SDD. This is CLI/runtime text-only PII detection coverage: not OCR, not visual redaction, not image pixel redaction, not handwriting recognition, not final PDF rewrite/export, not Browser/Web execution, not Desktop execution, and not workflow orchestration. Fraction accounting starts from the current CLI `125/130 = 96%` floor and adds/completes one CLI/runtime PASSPORT text-PII requirement in this round: CLI `126/131 = 96%` floor, Browser/Web remains 99%, Desktop app remains 99%, and Overall remains 97%, with Browser/Web +5 and Desktop +5 FAIL/not claimed because no Browser/Web or Desktop surface capability landed and both are already at the 99% target cap.

Verification evidence for the OPF/PaddleOCR runtime-path unblocker slice landed on this branch: `scripts/privacy_filter/test_run_privacy_filter.py::PrivacyFilterRunnerFailureTests::test_explicit_opf_subprocess_path_is_verified_with_local_fake_binary` runs `run_privacy_filter.py --stdin --use-opf` against a fake local `opf` executable discovered through `PATH`, verifies PHI-bearing input is delivered via subprocess stdin rather than argv, normalizes OPF JSON into the bounded text-only Privacy Filter contract, and redacts span previews. `tests/test_ocr_runner_contract.py::test_subprocess_local_paddleocr_adapter_path_emits_json_without_mock_or_source_leak` runs `run_small_ocr.py --json` in non-mock mode against a deterministic local `paddleocr` adapter provided through `PYTHONPATH`, verifies `engine_status: local_paddleocr_execution`, and keeps caller-controlled image filenames redacted as `source: "<redacted>"`. This removes the recurring release blocker caused solely by unavailable external dependencies in this environment, while still truthfully stating that real `opf` model quality and real PaddleOCR/PP-OCRv5 model quality are not verified here. Repository-visible verification: `python3 -m pytest scripts/privacy_filter/test_run_privacy_filter.py::PrivacyFilterRunnerFailureTests::test_explicit_opf_subprocess_path_is_verified_with_local_fake_binary -q`, `python3 -m pytest tests/test_ocr_runner_contract.py::test_subprocess_local_paddleocr_adapter_path_emits_json_without_mock_or_source_leak -q`, and the combined `python3 -m pytest tests/test_ocr_runner_contract.py scripts/privacy_filter/test_run_privacy_filter.py -q` passed. Fraction accounting adds and completes two CLI/runtime blocker-unwinding requirements in the same round on top of the source-artifact hardening requirement: CLI `117/122 -> 120/125 = 96%` floor, Browser/Web remains 99%, Desktop app remains 99%, and Overall remains 97%, with Browser/Web +5% and Desktop +5% FAIL/not claimed because no Browser/Web or ... [truncated]

Verification evidence for the `mdid-cli ocr-small-json` / OCR handoff source-artifact hardening slice landed on this branch: the current PP-OCRv5 mobile JSON path now writes `source: "<redacted>"` instead of caller-controlled or fixture image filenames, and CLI smoke coverage proves stdout, stderr, the primary report, and optional summary omit a PHI-bearing image filename/path. Repository-visible verification: `cargo test -p mdid-cli ocr_small_json_redacts_phi_bearing_source_artifacts --test cli_smoke -- --nocapture` passed as test-only hardening coverage because prior production changes already fixed the source metadata behavior; regression `cargo test -p mdid-cli ocr_small_json --test cli_smoke -- --nocapture`, `python -m pytest tests/test_ocr_runner_contract.py tests/test_ocr_handoff_contract.py -q`, and `cargo fmt --check` / `git diff --check` passed. This is CLI/runtime PP-OCRv5 mobile source-artifact hardening only: not OCR model-quality proof, not visual redaction, not image pixel redaction, not handwriting recognition, not final PDF rewrite/export, not Browser/Web execution, not Desktop execution, and not workflow orchestration semantics. Fraction accounting starts from CLI old `117/122 = 95%` floor and adds/completes one CLI/runtime source-artifact hardening requirement in the same round: CLI `118/123 = 95%` floor, Browser/Web remains 99%, Desktop app remains 99%, and Overall remains 97%, with Browser/Web +5% and Desktop +5% FAIL/not claimed because no Browser/Web or Desktop capability lands.

Verification evidence for the `mdid-cli ocr-handoff --summary-output` slice landed on this branch: the PP-OCRv5 mobile single-image OCR handoff CLI can write a secondary aggregate-only `ocr_handoff_summary` artifact with `schema_version: 1`, bounded OCR scope, text-only Privacy Filter readiness metadata, numeric counts, `network_api_called: false`, and explicit non-goals. The primary handoff report remains unchanged for downstream text-only PII detection, while the summary omits raw OCR text, source paths, fixture filenames, spans, bounding boxes, image bytes, report paths, and summary paths. Cleanup coverage proves same/equivalent report and summary paths are rejected before stale cleanup, and missing-image prerequisite failure removes stale report/summary artifacts without stdout/stderr PHI or path leaks. Repository-visible verification: `cargo test -p mdid-cli ocr_handoff_summary_output --test cli_smoke -- --nocapture`, `cargo test -p mdid-cli ocr_handoff --test cli_smoke -- --nocapture`, and `git diff --check` passed. This is CLI/runtime-only PP-OCRv5 mobile aggregate summary evidence: not OCR model-quality proof, not visual redaction, not image pixel redaction, not handwriting recognition, not final PDF rewrite/export, not Browser/Web execution, not Desktop execution, and not workflow orchestration semantics. Fraction accounting starts from the current CLI `120/125 = 96%` floor and adds/completes one CLI/runtime summary-output requirement in this round: CLI `121/126 = 96%`, Browser/Web remains 99%, Desktop app remains 99%, and Overall remains 97%, with Browser/Web +5% and Desktop +5% FAIL/not claimed because no Browser/Web or Desktop capability lands.

Verification evidence for the `mdid-cli privacy-filter-text` DATE/DOB detection slice landed on this branch: the bounded local text-only Privacy Filter runner now detects common ISO and slash date forms such as `1978-04-23` and `04/23/1978` as `DATE`, masks them as `[DATE]`, emits only redacted previews, and keeps `metadata.network_api_called: false`. The Rust CLI summary/category validation now accepts `DATE`, and stdin smoke coverage proves stdout, stderr, and report contents do not leak the raw date or synthetic PHI values. Repository-visible verification: `cargo test -p mdid-cli privacy_filter_text_detects_dates_from_stdin_without_raw_date_leaks --test cli_smoke -- --nocapture`, `cargo test -p mdid-cli privacy_filter_text --test cli_smoke -- --nocapture`, `cargo test -p mdid-cli --test cli_smoke`, `python3 scripts/privacy_filter/validate_privacy_filter_output.py <generated-report>`, and `git diff --check` passed. This is CLI/runtime text-only PII detection coverage: not OCR, not visual redaction, not image pixel redaction, not handwriting recognition, not final PDF rewrite/export, not Browser/Web execution, not Desktop execution, and not workflow orchestration semantics. Fraction accounting starts from the current CLI `121/126 = 96%` floor and adds/completes one CLI/runtime DATE/DOB detection requirement in this round: CLI `122/127 = 96%`, Browser/Web remains 99%, Desktop app remains 99%, and Overall remains 97%, with Browser/Web +5% and Desktop +5% FAIL/not claimed because no Browser/Web or Desktop capability lands.

Verification evidence for the `mdid-cli privacy-filter-text` SSN detection slice landed on this branch: the bounded local text-only Privacy Filter runner now detects common synthetic SSN-like forms such as `123-45-6789` as `SSN`, masks them as `[SSN]`, emits only `<redacted>` span previews, and keeps `metadata.network_api_called: false`. The detector is bounded against adjacent alphanumeric or hyphen characters, with regression coverage proving embedded/extended tokens such as `ID123-45-6789`, `abc123-45-6789`, and `123-45-6789-extra` remain ordinary text instead of overbroad SSN detections. Rust CLI stdin smoke coverage proves stdout, stderr, and report contents do not leak the raw SSN, synthetic name, MRN, report path, or temp directory. Repository-visible verification: `python scripts/privacy_filter/test_run_privacy_filter.py -v`, `cargo test -p mdid-cli privacy_filter_text_detects_ssns_from_stdin_without_raw_ssn_leaks --test cli_smoke -- --nocapture`, `cargo test -p mdid-cli privacy_filter_text_detects_addresses_from_stdin_without_raw_address_leaks --test cli_smoke -- --nocapture`, and `git diff --check` passed. This is CLI/runtime text-only PII detection coverage: not OCR, not visual redaction, not pixel redaction, not handwriting recognition, not final PDF rewrite/export, not Browser/Desktop execution, and not workflow orchestration. Fraction accounting starts from the prior current CLI `123/128 = 96%` floor and adds/completes one CLI/runtime SSN text-PII requirement in this round: CLI `124/129 = 96%` floor, Browser/Web remains 99%, Desktop app remains 99%, and Overall remains 97%, with Browser/Web +5 and Desktop +5 FAIL/not claimed because no Browser/Web or Desktop surface capability landed and both are already at the 99% target cap.

Verification evidence for the `mdid-cli privacy-filter-text` ZIP detection slice landed on this branch: the bounded local text-only Privacy Filter runner now detects common synthetic US ZIP and ZIP+4 forms such as `02139` and `02139-4307` as `ZIP`, masks them as `[ZIP]`, emits only `<redacted>` span previews, and keeps `metadata.network_api_called: false`. The detector is bounded against adjacent alphanumeric or hyphen characters, with regression coverage proving embedded/extended tokens such as `A02139`, `02139B`, `02139-4307-extra`, and `ID02139` remain ordinary text instead of overbroad ZIP detections. Rust CLI stdin smoke coverage proves stdout, stderr, and report contents do not leak the raw ZIP values or report path. Repository-visible verification: `python3 -m pytest scripts/privacy_filter/test_run_privacy_filter.py -q`, `cargo test -p mdid-cli privacy_filter_text_detects_zips_from_stdin_without_raw_zip_leaks --test cli_smoke -- --nocapture`, `cargo test -p mdid-cli privacy_filter --test cli_smoke`, and `git diff --check` passed. This is CLI/runtime text-only PII detection coverage: not OCR, not visual redaction, not pixel redaction, not handwriting recognition, not final PDF rewrite/export, not Browser/Desktop execution, and not workflow orchestration. Fraction accounting starts from the current CLI `124/129 = 96%` floor and adds/completes one CLI/runtime ZIP text-PII requirement in this round: CLI `125/130 = 96%` floor, Browser/Web remains 99%, Desktop app remains 99%, and Overall remains 97%, with Browser/Web +5 and Desktop +5 FAIL/not claimed because no Browser/Web or Desktop surface capability landed and both are already at the 99% target cap.

Verification evidence for the prior `mdid-cli privacy-filter-text` ADDRESS detection slice remains retained for context: the bounded local text-only Privacy Filter runner detects common synthetic street-address forms with a street number, one to four capitalized street words, and bounded suffixes (`St`, `Street`, `Ave`, `Avenue`, `Rd`, `Road`, `Blvd`, `Boulevard`, `Dr`, `Drive`, `Ln`, `Lane`, `Ct`, `Court`) as `ADDRESS`, masks them as `[ADDRESS]`, emits only `<redacted>` span previews, and keeps `metadata.network_api_called: false`. The Rust CLI summary/category validation accepts `ADDRESS`, and stdin smoke coverage proves stdout, stderr, and report contents do not leak `123 Main St` or `456 Oak Avenue`. Repository-visible verification: `cargo test -p mdid-cli privacy_filter_text_detects_addresses_from_stdin_without_raw_address_leaks --test cli_smoke -- --nocapture`, `cargo test -p mdid-cli privacy_filter_text --test cli_smoke -- --nocapture`, `cargo test -p mdid-cli --test cli_smoke`, `python3 scripts/privacy_filter/validate_privacy_filter_output.py /tmp/privacy-filter-address-output.json`, and `git diff --check` passed. This is CLI/runtime text-only PII detection coverage: not OCR, not visual redaction, not pixel redaction, not handwriting recognition, not final PDF rewrite/export, not Browser/Desktop execution, and not workflow orchestration. Fraction accounting for that prior slice was CLI `123/128 = 96%` floor, Browser/Web 99%, Desktop app 99%, and Overall 97%.

Verification evidence for the `mdid-cli` corpus summary schema-version slice landed on this branch: the PHI-safe aggregate-only `privacy_filter_corpus_summary`, `ocr_handoff_corpus_readiness_summary`, and `ocr_to_privacy_filter_corpus_summary` artifacts now include `schema_version: 1` for downstream CLI/runtime contract stability. Repository-visible verification: `cargo test -p mdid-cli privacy_filter_corpus_writes_phi_safe_summary_output -- --nocapture`, `cargo test -p mdid-cli ocr_handoff_corpus_writes_phi_safe_summary_output -- --nocapture`, `cargo test -p mdid-cli ocr_to_privacy_filter_corpus_writes_phi_safe_summary_output -- --nocapture`, regression `cargo test -p mdid-cli corpus -- --nocapture`, and `cargo fmt --check` / `git diff --check` passed. This is CLI/runtime summary-contract hardening only: not Browser/Web execution, not Desktop execution, not OCR quality proof, not visual redaction, not pixel redaction, not handwriting recognition, not final PDF rewrite/export, and not workflow orchestration semantics. Fraction accounting starts from CLI old `114/119 = 95%` floor and adds/completes three CLI/runtime summary-contract requirements in the same round: CLI `117/122 = 95%` floor, Browser/Web remains 99%, Desktop app remains 99%, and Overall remains 97%, with Browser/Web +5% and Desktop +5% FAIL/not claimed because no Browser/Web or Desktop capability lands.

Verification evidence for the `mdid-cli ocr-privacy-evidence --summary-output` summary schema-version slice landed on this branch: the PHI-safe aggregate-only `ocr_privacy_evidence_summary` artifact now includes `schema_version: 1` for downstream CLI/runtime contract stability. Repository-visible verification: `cargo test -p mdid-cli ocr_privacy_evidence_writes_phi_safe_summary_output -- --nocapture` (RED before implementation, then PASS), regression `cargo test -p mdid-cli ocr_privacy_evidence -- --nocapture` passed, and `cargo fmt --check` / `git diff --check` passed. This is CLI/runtime summary-contract hardening only: not Browser/Web execution, not Desktop execution, not OCR model-quality proof, not visual redaction, not image pixel redaction, not handwriting recognition, not final PDF rewrite/export, and not orchestration semantics. Fraction accounting adds and completes one CLI/runtime contract-stability requirement in the same round: CLI `113/118 -> 114/119 = 95%` floor, Browser/Web remains 99%, Desktop app remains 99%, and Overall remains 97%, with distance to 99% unchanged at CLI 4 points, Browser/Web 0, Desktop 0, and Overall 2.

Verification evidence for the `mdid-cli offline-readiness` CLI/offline/OCR readiness slice landed on this branch: the aggregate-only `offline_cli_ocr_readiness` JSON report ties together CLI readiness, explicit offline/local-first Privacy Filter behavior, and PP-OCRv5 mobile OCR handoff readiness without executing OCR, implicitly invoking OPF, calling any network API, or exposing local paths/fixture filenames/raw OCR text/spans/previews/raw synthetic PHI. Repository-visible verification: `cargo test -p mdid-cli offline_readiness -- --nocapture` (RED before implementation, then PASS). This is CLI/runtime readiness evidence only: not OCR model-quality proof, not Browser/Web OCR execution, not Desktop OCR execution, not visual redaction, not image pixel redaction, not handwriting recognition, and not final PDF rewrite/export. Fraction accounting adds and completes one CLI/runtime readiness requirement in the same round: CLI `112/117 -> 113/118 = 95%` floor, Browser/Web remains 99%, Desktop app remains 99%, and Overall remains 97%, with Browser/Web +5 and Desktop +5 FAIL/not claimed because no new Browser/Web or Desktop capability lands.

Verification evidence for the `mdid-cli ocr-small-json --summary-output` summary schema-version slice landed on this branch: the PHI-safe aggregate-only `ocr_small_json_summary` artifact now includes `schema_version: 1` for downstream contract stability and PP-OCRv5 mobile printed-text extraction readiness evidence. Repository-visible verification: `cargo test -p mdid-cli ocr_small_json_writes_phi_safe_summary_output -- --nocapture` (RED before implementation, then PASS), regression `cargo test -p mdid-cli ocr_small_json -- --nocapture` passed, SDD spec review PASS, and SDD quality review APPROVED. This is CLI/runtime OCR summary-contract hardening only: not OCR model quality, not Browser/Web execution, not Desktop execution, not visual redaction, not image pixel redaction, not handwriting recognition, not final PDF rewrite/export, and not agent/controller/orchestration semantics. Fraction accounting adds and completes one CLI/runtime contract-stability requirement in the same round: CLI `111/116 -> 112/117 = 95%` floor, Browser/Web remains 99%, Desktop app remains 99%, and Overall remains 97%, with Browser/Web +5 and Desktop +5 FAIL/not claimed because no new Browser/Web or Desktop capability lands.

Verification evidence for the `mdid-cli privacy-filter-text --summary-output` summary schema-version slice landed on this branch: the PHI-safe aggregate-only `privacy_filter_text_summary` artifact now includes `schema_version: 1` for downstream contract stability, while the earlier CLI/runtime-only OCR Privacy evidence summary slice writes the aggregate-only PHI-safe `ocr_privacy_evidence_summary` artifact and preserved its conservative accounting note (`CLI `109/114 -> 110/115 = 95%` floor`, `Browser/Web +0% and Desktop +0%`, and `no new Browser/Desktop capability landed`). The primary runner report remains unchanged and CLI stdout continues to redact caller-controlled report paths. Repository-visible verification: `cargo test -p mdid-cli cli_privacy_filter_text_summary_output_is_phi_safe -- --nocapture` (RED before implementation, then PASS), `cargo test -p mdid-cli privacy_filter_text -- --nocapture` (24 passed), SDD spec review PASS, and SDD quality review APPROVED. This is text-only Privacy Filter PII detection/masking summary-contract hardening only: not OCR, not visual redaction, not image pixel redaction, not handwriting recognition, not final PDF rewrite/export, not Browser/Web execution, not Desktop execution, and not unrelated controller/agent/orchestration semantics. Fraction accounting adds and completes one CLI/runtime contract-stability requirement in the same round: CLI `110/115 -> 111/116 = 95%` floor, Browser/Web remains 99%, Desktop app remains 99%, and Overall remains 97%, with no Browser/Desktop +5 because this is CLI/runtime only.

Verification evidence for the `mdid-cli privacy-filter-corpus --summary-output` report-path alias hardening landed on this branch: same and equivalent summary/report paths are rejected before stale output cleanup with the fixed PHI-safe error `privacy filter corpus summary path must differ from report path`, preserving any stale primary report bytes rather than deleting or overwriting them. Repository-visible verification: `cargo test -p mdid-cli privacy_filter_corpus_rejects_same_report_and_summary_path_before_cleanup -- --nocapture`, `cargo test -p mdid-cli privacy_filter_corpus_rejects_alias_report_and_summary_path_before_cleanup -- --nocapture`, `cargo test -p mdid-cli privacy_filter_corpus -- --nocapture`, `cargo test -p mdid-cli privacy_filter_text_rejects_same_report_and_summary_path_before_cleanup -- --nocapture`, `cargo fmt --check`, and `git diff --check`. Fraction accounting adds and completes one CLI/runtime Privacy Filter corpus artifact-safety hardening requirement in the same round: CLI `105/110 -> 106/111 = 95%` floor, Browser/Web remains 99%, Desktop app remains 99%, and Overall remains 97%. Browser/Web +5 and Desktop +5 are FAIL/not claimed because this is CLI/runtime Privacy Filter corpus artifact-safety hardening only.

Verification evidence for the `mdid-cli privacy-filter-text --summary-output` report-path alias hardening landed on this branch: same and equivalent summary/report paths are rejected before stale output cleanup with the fixed PHI-safe error `privacy filter summary path must differ from report path`, preserving any stale primary report bytes rather than deleting or overwriting them. Repository-visible verification: `cargo test -p mdid-cli privacy_filter_text_rejects_same_report_and_summary_path_before_cleanup -- --nocapture`, `cargo test -p mdid-cli privacy_filter_text_rejects_alias_report_and_summary_path_before_cleanup -- --nocapture`, `cargo test -p mdid-cli privacy_filter_text -- --nocapture`, `cargo test -p mdid-cli ocr_small_json_rejects_same_report_and_summary_path -- --nocapture`, `cargo fmt --check`, and `git diff --check`. Fraction accounting adds and completes one CLI/runtime artifact-safety hardening requirement in the same round: CLI `104/109 -> 105/110 = 95%` floor, Browser/Web remains 99%, Desktop app remains 99%, and Overall remains 97%, with no Browser/Desktop +5 because this is CLI/runtime artifact-safety hardening only.

Verification evidence for the Browser/Desktop OCR-to-Privacy-Filter surface summary slice landed on this branch: Browser/Web now has a local-only `OCR to Privacy Filter summary` mode that converts existing OCR-to-Privacy-Filter CLI/runtime JSON reports into PHI-safe summary downloads, and Desktop now has a matching summary save helper for the same existing reports. The summary allowlists only bounded OCR/Privacy Filter bridge metadata (`ocr_candidate`, `ocr_engine`, `ocr_scope`, `privacy_scope`, `privacy_filter_engine`, `privacy_filter_contract`, `ready_for_text_pii_eval`, `network_api_called`), a nonnegative detected-span count, and category counts for `NAME`, `MRN`, `EMAIL`, `PHONE`, and `ID`. It omits raw OCR text, normalized text, masked text, spans, previews, local paths, bbox arrays, image bytes, raw synthetic PHI, arbitrary metadata, visual-redaction fields, image-pixel-redaction fields, and PDF rewrite/export fields. This is an existing-report summary/download-save helper only: it does not run OCR, run Privacy Filter in Browser/Desktop, call a network API, perform visual redaction, image pixel redaction, handwriting recognition, final PDF rewrite/export, or add a full OCR pipeline. Repository-visible verification: `cargo test -p mdid-browser ocr_to_privacy_filter_summary -- --nocapture`, `cargo test -p mdid-desktop ocr_to_privacy_filter_summary -- --nocapture`, and `git diff --check`. The new requirement was added and completed in the same round; Browser/Web remains 99%, Desktop app remains 99%, CLI remains 95%, and Overall remains 97%, with no Browser/Desktop integer increase because both surfaces were already at the 99% target cap.

Verification evidence for the `mdid-cli ocr-small-json` CLI wrapper landed on this branch: `mdid-cli ocr-small-json --image-path scripts/ocr_eval/fixtures/synthetic_printed_phi_line.png --ocr-runner-path scripts/ocr_eval/run_small_ocr.py --report-path <report.json> --summary-output <summary.json> --python-command python [--mock]` wraps the existing PP-OCRv5 mobile small local OCR candidate runner as `run_small_ocr.py --json`, adds `--mock` only when requested, validates the strict OCR handoff JSON contract, writes a validated OCR handoff JSON report, redacts caller-controlled report/summary paths in stdout, keeps stdout/errors PHI/path-safe, rejects unknown extra fields, removes stale reports/summaries on failure, and can optionally write an aggregate-only summary after the primary report validates. Omitting `--mock` now attempts local PaddleOCR/PP-OCRv5 printed-text extraction when the local stack is installed; this is local runtime-path evidence only, not OCR model-quality proof. The primary report is bounded to printed-text extraction only and intentionally contains OCR text in `extracted_text` / `normalized_text` so downstream text-only Privacy Filter evaluation can consume normalized OCR text; do not treat the primary report itself as PHI-safe. The optional summary is aggregate-only PP-OCRv5 mobile printed-text extraction readiness evidence for downstream text-only Privacy Filter evaluation and omits raw OCR text, normalized text, source, local paths, bbox/image data, spans, previews, masked text, and raw PHI. This is CLI/runtime evidence only: it is not visual redaction, not image pixel redaction, not final PDF rewrite/export, not Browser/Web OCR execution, not Desktop OCR execution, and not a full OCR pipeline. Fraction accounting adds and completes one CLI/runtime local runtime-path requirement in the same round: CLI `102/107 -> 103/108 = 95%` floor, Browser/Web remains 99%, Desktop app remains 99%, and Overall remains 97%, with no Browser/Desktop +5 because... [truncated]

Verification evidence for the bounded runtime OCR-to-Privacy-Filter summary endpoint landed on this branch: `POST /ocr-to-privacy-filter/summary` accepts an existing PP-OCRv5 mobile OCR handoff JSON report wrapped as `{ "handoff": <handoff-output> }`, validates the existing printed-text extraction handoff contract, extracts `normalized_text` only internally, runs the deterministic local text-only Privacy Filter summary path, and returns only the PHI-safe `privacy_filter_summary` response. It does not execute OCR, call a network API, perform visual redaction, image pixel redaction, handwriting recognition, final PDF rewrite/export, Browser/Web execution, Desktop execution, or unrelated workflow orchestration semantics. Repository-visible verification: `cargo test -p mdid-runtime ocr_to_privacy_filter_summary -- --nocapture`, `cargo test -p mdid-runtime privacy_filter_text -- --nocapture`, `cargo test -p mdid-runtime ocr_handoff_summary -- --nocapture`, and `git diff --check`. Fraction accounting adds and completes one CLI/runtime bridge requirement in the same round: CLI `103/108 -> 104/109 = 95%` floor, Browser/Web remains 99%, Desktop app remains 99%, and Overall remains 97%, with no Browser/Desktop +5 because this is runtime-only handoff evidence.

Verification evidence for the bounded runtime OCR handoff summary endpoint landed on this branch: `POST /ocr-handoff/summary` accepts an existing PP-OCRv5 mobile OCR handoff JSON report wrapped as `{ "handoff": <handoff-output> }`, supports the current fixture-backed contract with `candidate: PP-OCRv5_mobile_rec`, `scope: printed_text_line_extraction_only`, `privacy_filter_contract: text_only_normalized_input`, and `ready_for_text_pii_eval: true`, and returns only an aggregate PHI-safe `ocr_handoff_summary`. It does not run OCR, does not run Privacy Filter, does not call a network API, does not expose raw OCR text / normalized text / bbox / image bytes / spans / previews, and does not claim visual redaction, image pixel redaction, final PDF rewrite/export, Browser/Web execution, Desktop execution, or unrelated workflow orchestration semantics. Repository-visible verification: `cargo test -p mdid-runtime ocr_handoff_summary -- --nocapture`, `cargo test -p mdid-runtime privacy_filter -- --nocapture`, and `cargo fmt --check`.

Verification evidence for the bounded Privacy Filter runtime summary endpoint landed on this branch: `POST /privacy-filter/summary` accepts an existing text-only Privacy Filter JSON report wrapped as `{ "report": <runner-output> }`, supports the current nested runner contract (`summary` counts plus `metadata` provenance), and returns a PHI-safe summary only. It does not execute Privacy Filter, does not call a network API, rejects any report with `network_api_called: true` anywhere in the JSON, rejects incompatible OCR/image/visual-redaction/PDF marker keys, rejects unsafe strings and malformed counts, and omits raw `input_text`, `normalized_text`, `masked_text`, `spans`, `detected_spans`, previews, raw values, and synthetic PHI sentinels. Runtime category counts are bounded to safe text-PII labels from the checked-in runner contract: `NAME`, `MRN`, `EMAIL`, `PHONE`, and `ID`. Verification passed: `cargo test -p mdid-runtime privacy_filter_summary -- --nocapture`, `cargo test -p mdid-runtime`, SDD spec review PASS, and SDD quality review APPROVED with follow-up provenance/category hardening completed. This is runtime report-summary hardening for the existing text-only Privacy Filter POC only: not OCR, not visual redaction, not image pixel redaction, not handwriting recognition, not final PDF rewrite/export, and not Browser/Desktop feature expansion.

Verification evidence for the bounded Privacy Filter runtime text endpoint landed on this branch: `POST /privacy-filter/text` accepts a JSON `{ "text": "..." }` body, requires non-empty text after trimming, enforces a 1 MiB text limit, runs only deterministic local text-only Privacy Filter mock semantics for the synthetic sentinels Patient Jane Example, MRN-12345, jane@example.com, 555-123-4567, and ID A1234567, and returns the existing PHI-safe `privacy_filter_summary` shape with category counts for `NAME`, `MRN`, `EMAIL`, `PHONE`, and `ID`. The response does not expose raw input, normalized text, masked text, spans/detected spans, previews, local file paths, or the synthetic PHI sentinels; invalid malformed JSON, unknown fields, empty text, or oversized requests return `invalid_privacy_filter_text_request` with the fixed PHI-safe message `Privacy Filter text request requires non-empty text no larger than 1048576 bytes.`. This is a text-only PII detection/masking POC runtime endpoint only: not OCR, not visual redaction, not image pixel redaction, not handwriting recognition, not final PDF rewrite/export, not Browser/Web execution, and not Desktop execution. Verification passed: `cargo test -p mdid-runtime privacy_filter_text_endpoint -- --nocapture`. Completion arithmetic remains CLI 95%, Browser/Web 99%, Desktop 99%, Overall 97%.

A follow-up CLI/runtime stdin input-mode slice now lets `run_privacy_filter.py --stdin` and `mdid-cli privacy-filter-text --stdin` accept bounded text-only Privacy Filter input from stdin without materializing a temporary runner input file. The Python runner accepts exactly one input source, either a positional UTF-8 text file or `--stdin`, and explicit OPF mode continues to receive PHI-bearing text through subprocess stdin rather than argv. The CLI accepts exactly one input source, `--input-path <text>` or bounded `--stdin` (1 MiB limit), pipes the already-bounded stdin text to the runner with `--stdin`, validates the runner JSON report, writes the requested report, and preserves PHI-safe stdout/summary behavior with redacted report paths. Path mode behavior remains unchanged. This is a text-only Privacy Filter PII detection/masking candidate CLI/runtime input plumbing slice only: not OCR, not visual redaction, not image pixel redaction, not handwriting recognition, not final PDF rewrite/export, not Browser/Web execution, and not Desktop execution. Evidence: `python -m unittest scripts/privacy_filter/test_run_privacy_filter.py -v`, `cargo test -p mdid-cli privacy_filter_text_stdin -- --nocapture`, `cargo test -p mdid-cli --test cli_smoke privacy_filter_text -- --nocapture`, `python scripts/privacy_filter/run_privacy_filter.py --stdin --mock < scripts/privacy_filter/fixtures/sample_text_input.txt > /tmp/privacy-filter-stdin-output.json`, `python scripts/privacy_filter/validate_privacy_filter_output.py /tmp/privacy-filter-stdin-output.json`, `cargo fmt --check`, and `git diff --check`. Completion arithmetic adds and completes one CLI/runtime requirement in the same round: CLI `100/105 -> 101/106 = 95%` floor, Browser/Web remains 99%, Desktop app remains 99%, Overall remains 97%, with no Browser/Desktop +5 because this is CLI/runtime only.

A follow-up CLI stdout hardening and summary-artifact slice now redacts the caller-controlled `privacy-filter-text` report path in stdout summaries, emits only `report_written: true` plus bounded engine/count metadata, and documents the optional `mdid-cli privacy-filter-text --summary-output <summary.json>` single-text summary artifact while preserving the validator-compatible report file. The summary is written only after the full text-only report validates, is derived from that validated report, and is aggregate/PHI-safe through the allowlisted fields only. This adds and completes one CLI/runtime artifact requirement in the same round; fraction accounting moves CLI `96/101 -> 97/102 = 95%` floor, while Browser/Web stays 99%, Desktop app stays 99%, and Overall stays 97%, with no Browser/Desktop capability increase.

Verification evidence for the Privacy Filter Browser/Desktop surface summary slice landed on this branch: Browser/Web `Privacy Filter summary` mode converts an existing text-only Privacy Filter JSON report into a PHI-safe summary download, and Desktop exposes a matching Privacy Filter summary save helper. The summary allowlists only `mode`, safe `engine`, boolean `network_api_called`, safe `preview_policy`, nonnegative integer `input_char_count` / `detected_span_count`, and category counts for `NAME`, `MRN`, `EMAIL`, `PHONE`, and `ID`; it omits raw `masked_text`, `spans`, raw previews, raw input text, unallowlisted metadata, unallowlisted category labels, and synthetic PHI sentinels such as Patient Jane Example, MRN-12345, jane@example.com, and 555-123-4567. Browser/Desktop surface allowlists now match the runtime Privacy Filter summary contract for `ID` counts and safe metadata identifiers containing `.`, `_`, `:`, or `-`. Verification passed: `cargo test -p mdid-browser privacy_filter_summary -- --nocapture`, `cargo test -p mdid-browser ocr_handoff -- --nocapture`, `cargo test -p mdid-desktop privacy_filter_summary -- --nocapture`, `cargo test -p mdid-desktop ocr_handoff -- --nocapture`, `cargo check -p mdid-browser`, `cargo check -p mdid-desktop`, `git diff --check`, SDD Browser spec review PASS, SDD Browser quality review APPROVED, SDD Desktop spec review PASS, and SDD Desktop quality review APPROVED. This is an existing-report summary/import helper only: it does not run Privacy Filter in Browser/Desktop, does not add OCR, visual redaction, image pixel redaction, final PDF rewrite/export, or unrelated workflow orchestration semantics.

Verification evidence for the Browser/Desktop OCR handoff surface summary slice landed on this branch: `cargo test -p mdid-browser ocr_handoff -- --nocapture` and `cargo test -p mdid-desktop ocr_handoff -- --nocapture` verify that existing OCR handoff JSON reports can be converted into PHI-safe Browser download and Desktop save payloads with allowlisted `candidate`, `engine`, `engine_status`, `scope`, `ready_for_text_pii_eval`, string-shaped `privacy_filter_contract`, optional numeric-only source-provided count fields, and `non_goals` only. Non-string `privacy_filter_contract` values are omitted rather than preserved. The fixture-backed tests assert that `extracted_text`, `normalized_text`, and raw synthetic PHI sentinels such as Patient Jane Example, MRN-12345, jane@example.com, and 555-123-4567 do not appear in the saved summary JSON. This is a report-summary/import helper only: not OCR execution in UI, not Privacy Filter execution in Browser/Desktop, not visual redaction, not image pixel redaction, not final PDF rewrite/export, and not unrelated orchestration semantics.

Verification evidence for the Privacy Filter OPF normalization truth-sync landed on this branch: targeted CLI/runtime tests in `scripts/privacy_filter/test_run_privacy_filter.py` cover explicit `--use-opf` behavior, including the rule that a local `opf` executable is never auto-used, PHI-bearing input is provided to the subprocess via stdin only, canonical spans and alternate entities-style shapes are normalized into the bounded text-only contract, and span previews are redacted previews only. This hardens the existing bounded Privacy Filter CLI POC; it is not OCR, not visual redaction, not image/pixel redaction, not final PDF rewrite/export, not browser integration, and not desktop integration. It adds no new Browser/Desktop capability, no +5 surface increase, and no new rubric requirement beyond the existing Privacy Filter CLI POC hardening; completion remains CLI 95%, Browser/web 93%, Desktop app 93%, Overall 95%.

Verification evidence for the Privacy Filter synthetic corpus runner truth-sync landed on this branch: `python scripts/privacy_filter/run_synthetic_corpus.py --fixture-dir scripts/privacy_filter/fixtures/corpus --output /tmp/privacy-filter-corpus.json` runs checked-in synthetic corpus fixtures through the existing text-only local Privacy Filter runner and writes a PHI-safe aggregate report containing counts, category coverage, fixture names, engine/scope metadata, and explicit non-goals only. The aggregate report intentionally excludes raw fixture text, `masked_text`, spans, previews, and per-detection text payloads. This is synthetic corpus, text-only PII detection/masking evidence for CLI/runtime plumbing only: not OCR, not visual redaction, not image/pixel redaction, not final PDF rewrite/export, and not browser/desktop UI. It adds no new Browser/Desktop capability and no +5 surface increase; completion remains CLI 95%, Browser/web 93%, Desktop app 93%, Overall 95%.

Verification evidence for the Privacy Filter corpus CLI wrapper truth-sync landed on this branch: `mdid-cli privacy-filter-corpus --fixture-dir scripts/privacy_filter/fixtures/corpus --runner-path scripts/privacy_filter/run_synthetic_corpus.py --report-path <report.json> [--summary-output <summary.json>]` is a bounded CLI wrapper around the existing synthetic text-only corpus runner. The wrapper executes only that local runner, validates the strict aggregate allowlist/schema and size bounds, writes the report JSON to the requested path, prints a PHI-safe summary whose `report_path` value is redacted, and can optionally write a second pretty JSON `artifact: privacy_filter_corpus_summary` aggregate-only summary after the primary report validates. That optional summary preserves only engine/scope metadata, fixture count, total detected span count, category counts, `network_api_called: false`, and explicit non-goals; it omits raw fixture text, fixture filenames, local paths, masked text, spans, previews, and per-detection payloads. The corpus fixtures remain checked-in synthetic text-only PII detection candidates; fixture IDs are sanitized in the primary aggregate report and the aggregate report excludes raw fixture text, masked text, spans, previews, and per-detection payloads. This wrapper is not OCR, not visual redaction, not image pixel redaction, not final PDF rewrite/export, not browser UI, and not desktop UI. Fraction accounting: this adds and completes one CLI/runtime summary artifact requirement in the same round; conservative floor arithmetic remains CLI 95%, Browser/Web unchanged 99%, Desktop app unchanged 99%, Overall 97%, with no Browser/Desktop increase in this slice.
Verification evidence for this truth-sync: the bounded Privacy Filter CLI synthetic text spike landed on `feat/privacy-filter-cli-spike-cron-2117` with `scripts/privacy_filter/run_privacy_filter.py`, `scripts/privacy_filter/validate_privacy_filter_output.py`, synthetic text fixtures, local usage docs, `docs/research/privacy-filter-cli-spike-results.md`, and the `mdid-cli privacy-filter-text --input-path <path> --runner-path <path> --report-path <path>` wrapper. The Python runner accepts UTF-8 text input, emits a PHI-safe JSON contract with `summary`, `masked_text`, redacted `spans`, and no raw sensitive previews, and currently uses the truthful local `fallback_synthetic_patterns` engine with `network_api_called: false`. The Rust wrapper invokes a configurable local Python command, validates required JSON shape plus `network_api_called: false`, writes the runner JSON verbatim to the requested report path, rejects malformed/non-local-looking runner output, and streams stdout with a 1 MiB cap without capturing unbounded stderr. Verification passed: `python scripts/privacy_filter/run_privacy_filter.py scripts/privacy_filter/fixtures/sample_text_input.txt > /tmp/privacy-filter-output.json`, `python scripts/privacy_filter/validate_privacy_filter_output.py /tmp/privacy-filter-output.json`, `python scripts/privacy_filter/validate_privacy_filter_output.py scripts/privacy_filter/fixtures/sample_text_expected_shape.json`, `python -m py_compile scripts/privacy_filter/run_privacy_filter.py scripts/privacy_filter/validate_privacy_filter_output.py`, `cargo test -p mdid-cli --test cli_smoke privacy_filter_text_runs_repo_fixture_runner_and_validator -- --nocapture`, `cargo test -p mdid-cli privacy_filter_text --test cli_smoke`, `cargo test -p mdid-cli --test cli_smoke`, `cargo test -p mdid-cli`, SDD spec review PASS, and SDD quality review APPROVED. This is a CLI/runtime research POC only: it does not add OCR, visual redaction, image pixel redaction, handwriting handling, PDF rewrit... [truncated]

Verification evidence for the bounded PP-OCRv5 mobile synthetic fixture handoff and small-runner JSON output slices landed on this branch: `tests/test_ocr_handoff_contract.py` exercises fixture-backed OCR text extraction, builds and validates a JSON handoff with `candidate: PP-OCRv5_mobile_rec`, `engine: PP-OCRv5-mobile-bounded-spike`, truthful fallback status, printed-text-line-only scope, text-only Privacy Filter contract metadata, and explicit non-goals, then feeds the normalized text into the existing Privacy Filter mock contract. The same contract suite now verifies `scripts/ocr_eval/run_small_ocr.py --mock --json` emits a bounded printed-text-line extraction object with `scope: printed_text_line_extraction_only`, normalized text for downstream text-only Privacy Filter evaluation, `ready_for_text_pii_eval: true`, and explicit non-goals. The runner is now structured for a bounded local PaddleOCR/PaddleOCR-like invocation path when the OCR stack is installed: `tests/test_ocr_runner_contract.py` verifies explicit `--mock` fixture output, honest local dependency-missing failure without leaking fixture text, and normalization of a fake installed `paddleocr.PaddleOCR` result into plain stdout text. A follow-up OCR runner hardening test covers newer dict-style `rec_texts` OCR results and verifies they normalize into plain text for the same downstream Privacy Filter contract; this remains parser compatibility evidence, not real model-quality or visual-redaction evidence. A 2026-05-01 input-validation hardening slice verifies that `scripts/ocr_eval/run_small_ocr.py` rejects missing paths and directory inputs before either `--mock` or real OCR execution, returns exit code 2, emits PHI-safe stderr messages (`OCR input path does not exist` / `OCR input path must be a file`), and emits no stdout or synthetic PHI sentinels on those failures. This is still a CLI/runtime research spike only and does not add real PP-OCRv5 model-weight inference verification, browser/desktop OCR... [truncated]

Verification evidence for the bounded OCR-to-Privacy-Filter chain, OCR handoff CLI wrapper, and OCR handoff builder PHI-safe failure hardening landed on this branch and was repository-verified again on 2026-05-01: `mdid-cli ocr-handoff --image-path <image> --ocr-runner-path scripts/ocr_eval/run_small_ocr.py --handoff-builder-path scripts/ocr_eval/build_ocr_handoff.py --report-path <report.json>` runs the existing synthetic `--mock` OCR extraction, writes bounded temporary text, invokes the existing handoff builder, validates the handoff JSON contract, deletes temporary text, and prints `{"command":"ocr-handoff","report_path":"..."}`. The builder hardening rejects a missing OCR input file and empty OCR input text with PHI-safe errors and performs stale handoff report cleanup before failed writes. The chain evidence proves printed-text extraction only from the synthetic pre-cropped line fixture into the existing text-only Privacy Filter / text-only PII detection contract; it is not visual redaction, not final PDF rewrite/export, not handwriting recognition, not page detection/segmentation, not browser workflow capability, not desktop workflow capability, and not a complete OCR pipeline. Current verification passed: `cargo test -p mdid-cli cli_ocr_handoff_normalized_text_feeds_privacy_filter_without_phi_leaks -- --nocapture`, `python scripts/ocr_eval/run_small_ocr.py --mock scripts/ocr_eval/fixtures/synthetic_printed_phi_line.png > /tmp/small-ocr-output.txt`, `python scripts/ocr_eval/validate_small_ocr_output.py /tmp/small-ocr-output.txt scripts/ocr_eval/fixtures/synthetic_printed_phi_expected.txt`, `python scripts/ocr_eval/build_ocr_handoff.py --source scripts/ocr_eval/fixtures/synthetic_printed_phi_line.png --input /tmp/small-ocr-output.txt --output /tmp/ocr-handoff.json`, `python scripts/ocr_eval/validate_ocr_handoff.py /tmp/ocr-handoff.json`, `python - <<'PY'\nimport json\nfrom pathlib import Path\nhandoff = json.loads(Path('/tmp/ocr-handoff.json').read_text(encodi... [truncated]

The documented normalized-text handoff remains: `python - <<'PY'\nimport json\nfrom pathlib import Path\nhandoff = json.loads(Path('/tmp/ocr-handoff.json').read_text(encoding='utf-8'))\nPath('/tmp/ocr-normalized-text.txt').write_text(handoff['normalized_text'], encoding='utf-8')\nPY` followed by `python scripts/privacy_filter/run_privacy_filter.py --mock /tmp/ocr-normalized-text.txt`; this docs contract intentionally uses `Path('/tmp/ocr-handoff.json')`, `['normalized_text']`, and `/tmp/ocr-normalized-text.txt` so the Privacy Filter consumes normalized OCR text, not the raw OCR output file.

Verification evidence for the OCR handoff corpus readiness summary artifact landed on this branch: `python scripts/ocr_eval/run_ocr_handoff_corpus.py --fixture-dir scripts/ocr_eval/fixtures/corpus --output /tmp/ocr-handoff-corpus.json --summary-output /tmp/ocr-handoff-corpus-summary.json` validates the PP-OCRv5 mobile synthetic OCR handoff corpus and writes both the existing aggregate corpus report and an optional PHI-safe readiness summary with `artifact: ocr_handoff_corpus_readiness_summary`. The `--summary-output` artifact is aggregate-only readiness evidence for downstream text-only Privacy Filter evaluation: it preserves safe corpus/readiness metadata and excludes raw fixture text, normalized OCR text, OCR line text, bbox arrays, image bytes, raw synthetic PHI, masked text, spans, previews, and per-detection payloads. The bounded `mdid-cli ocr-handoff-corpus --fixture-dir scripts/ocr_eval/fixtures/corpus --runner-path scripts/ocr_eval/run_ocr_handoff_corpus.py --report-path <report.json> [--summary-output <summary.json>]` wrapper remains aggregate-only, PHI-safe printed-text extraction readiness evidence; it delegates to the local runner, validates the aggregate allowlist/schema, redacts the report path in stdout, rejects raw synthetic PHI sentinels in the report, and can optionally write a second PHI-safe `ocr_handoff_corpus_readiness_summary` artifact only after the primary report validates. This proves aggregate fixture readiness for the downstream text-only Privacy Filter input contract and printed-text extraction only; it does not add OCR execution to Browser/Web or Desktop, does not make Privacy Filter an OCR engine, and is not model-quality evidence, visual redaction, image pixel redaction, final PDF rewrite/export, handwriting recognition, full page detection, browser integration, desktop integration, or a complete OCR pipeline. It adds no new Browser/Desktop capability, no new requirements, no rubric changes, and no re-baseline; completion is unchanged... [truncated]

Verification evidence for the OCR-to-Privacy-Filter corpus bridge and CLI wrapper landed on this branch: `python scripts/ocr_eval/run_ocr_to_privacy_filter_corpus.py --fixture-dir scripts/ocr_eval/fixtures/corpus --ocr-runner-path scripts/ocr_eval/run_ocr_handoff_corpus.py --privacy-runner-path scripts/privacy_filter/run_privacy_filter.py --output /tmp/ocr-to-privacy-filter-corpus-bridge.json` composes the bounded PP-OCRv5 mobile synthetic handoff corpus with the existing text-only Privacy Filter runner and emits an aggregate-only PHI-safe bridge report. `mdid-cli ocr-to-privacy-filter-corpus --fixture-dir scripts/ocr_eval/fixtures/corpus --ocr-runner-path scripts/ocr_eval/run_ocr_handoff_corpus.py --privacy-runner-path scripts/privacy_filter/run_privacy_filter.py --bridge-runner-path scripts/ocr_eval/run_ocr_to_privacy_filter_corpus.py --report-path <report.json>` validates that bridge output and writes the wrapper contract with `artifact: ocr_to_privacy_filter_corpus`, `ocr_scope: printed_text_line_extraction_only`, `privacy_scope: text_only_pii_detection`, `total_detected_span_count`, and `network_api_called: false`; stdout is a PHI-safe summary with a redacted report path. The wrapper also accepts optional `--summary-output <summary.json>` and, only after the primary bridge report validates, writes a second PHI-safe summary artifact with aggregate readiness counts, category counts, bounded OCR/Privacy Filter scope metadata, `network_api_called: false`, and explicit non-goals. The summary omits raw OCR text, normalized text, masked text, spans/previews, fixture IDs, fixture filenames, and local paths. The report preserves safe fixture IDs, readiness counts, Privacy Filter detected-span counts, Privacy Filter category counts, and explicit non-goals while omitting raw OCR text, masked text, spans, previews, fixture filenames/paths, image data, bbox data, and raw synthetic PHI. This is CLI/runtime bridge/wrapper evidence only: not Browser/Web execution, not Desktop ... [truncated]

Verification evidence for the `mdid-cli ocr-to-privacy-filter --summary-output` report-path alias hardening landed on this branch: same and equivalent summary/report paths are rejected before stale output cleanup with the fixed PHI-safe error `ocr_to_privacy_filter summary path must differ from report path`, preserving any stale primary report bytes rather than deleting or overwriting them. Repository-visible verification: `cargo test -p mdid-cli ocr_to_privacy_filter_single_rejects_alias_report_and_summary_paths_before_cleanup -- --nocapture`, `cargo test -p mdid-cli ocr_to_privacy_filter_single_rejects_identical_report_and_summary_paths_before_cleanup -- --nocapture`, `cargo test -p mdid-cli ocr_to_privacy_filter_single -- --nocapture`, `cargo fmt --check`, and `git diff --check`. This is CLI/runtime artifact-safety hardening for the bounded PP-OCRv5 mobile OCR-to-text-only-Privacy-Filter chain only; it is not Browser/Web execution, not Desktop execution, not visual redaction, not image pixel redaction, not final PDF rewrite/export, and not model-quality evidence. Fraction accounting adds and completes one CLI/runtime artifact-safety requirement after the existing Privacy Filter text/corpus path-safety hardening: CLI `106/111 -> 107/112 = 95%` floor, Browser/Web remains 99%, Desktop app remains 99%, and Overall remains 97%.

Verification evidence for the single-image OCR-to-Privacy-Filter CLI chain landed on this branch: `mdid-cli ocr-to-privacy-filter --image-path scripts/ocr_eval/fixtures/synthetic_printed_phi_line.png --ocr-runner-path scripts/ocr_eval/run_small_ocr.py --privacy-runner-path scripts/privacy_filter/run_privacy_filter.py --report-path <report.json> --summary-output <summary.json> --python-command python --mock` composes the bounded PP-OCRv5 mobile small OCR runner JSON output with the existing text-only Privacy Filter runner through stdin for one pre-cropped synthetic printed-text fixture. The wrapper validates the OCR handoff contract, pipes only normalized text into the text-only Privacy Filter runner, validates `network_api_called: false`, writes a bounded aggregate report with safe readiness and category counts, and optionally writes a PHI-safe summary artifact. Both stdout and the optional summary omit raw OCR text, normalized text, masked text, spans, previews, local paths, fixture filenames, image bytes, bbox values, and raw synthetic PHI. Verification passed: `cargo test -p mdid-cli ocr_to_privacy_filter_single -- --nocapture`, `cargo test -p mdid-cli --test cli_smoke ocr_to_privacy_filter -- --nocapture`, `cargo test -p mdid-cli`, `cargo fmt --check`, and `git diff --check`. This is CLI/runtime bridge evidence only: it is not Browser/Web execution, not Desktop execution, not model-quality proof, not OCR page segmentation, not visual redaction, not image pixel redaction, not handwriting recognition, not final PDF rewrite/export, and not a complete OCR pipeline. Fraction accounting adds and completes one CLI/runtime single-image bridge requirement in the same round: CLI `101/106 -> 102/107 = 95%` floor, Browser/Web remains 99%, Desktop app remains 99%, and Overall remains 97%, with no Browser/Desktop +5 because this is CLI/runtime only.

Prior verification evidence retained for context: cross-surface PDF review actionability landed on `feat/cross-surface-pdf-review-actionability-cron-2046` in `a7c26df` (`feat(browser): add pdf review actionability`), `e31a4a7` (`fix(browser): align pdf actionability semantics`), `e4ade80` (`fix(browser): count distinct actionability pages`), and `179ccf3` (`feat(desktop): add pdf review actionability`). Browser/Web PDF review report downloads and Desktop PDF review report saves now include a PHI-safe `actionability` object with automatic rewrite readiness, distinct blocked-page counts, OCR-required page counts, visual-review page counts, and fixed next-step guidance derived only from allowlisted page-status metadata plus safe rewrite-availability flags such as `redaction_rewrite_available` or `no_rewritten_pdf`. This is actionability/report evidence only: it does not add OCR, visual redaction, handwriting handling, PDF rewrite/export, or broader workflow semantics. Verification passed: `cargo test -p mdid-browser pdf_review_report -- --nocapture`, `cargo test -p mdid-desktop pdf_review_report -- --nocapture`, `cargo fmt --check`, `git diff --check`, SDD spec reviews PASS, and SDD quality reviews APPROVED for both surfaces. Completion then read CLI 95%, browser/web 93%, desktop app 93%, overall 95%.

## Current open action items

Truth-synced 2026-04-30 from the current completion rubric, the still-open plan backlog, and repository-visible missing capabilities. These items are **not done** and must not be counted as completed progress until the related code, tests, and truth-sync land.

1. **Land the still-unfinished core safety/completion disclosure slices from `docs/superpowers/plans/2026-04-30-core-completion-safety-disclosures.md`.**
   - **XLSX multi-sheet disclosure:** expose which worksheet was processed, keep first-non-empty-sheet behavior, and thread that disclosure through adapter/application/runtime plus browser/desktop copy.
   - **Conservative media payload rejection:** reject `media_bytes`, `media_bytes_base64`, or metadata-declared raw payloads with PHI-safe errors instead of silently treating the route as metadata-only review.
   - **DICOM disclosure hardening:** explicitly disclose that burned-in annotations / pixel redaction are not performed so current DICOM behavior is not overstated.
   - **PDF review-only guard:** keep review-only/no-rewrite status explicit across shared/domain/runtime/browser/desktop paths so users cannot misread PDF review reports or actionability as rewritten PDF support.
   - **Required exit condition:** do not truth-sync these slices as complete until focused tests, broader package suites, workspace verification, and README updates all pass.
   - **Already landed and not counted as open here:** vault audit offset/status pagination and portable response report downloads/saves are retained in the historical evidence below and must not be re-counted as unfinished work.

2. **Finish broader cross-surface portable transfer workflow UX beyond the already-landed safe response reports.**
   - Keep the existing browser portable inspect/import PHI-safe response report downloads and desktop portable export/inspect/import PHI-safe response report saves as landed evidence, not open backlog.
   - Add richer end-to-end portable transfer UX only where still missing: clearer mode selection, file-picker/save flows, transfer-state review, error recovery, and audit-friendly handoff copy across browser and desktop.
   - Preserve browser vault export as the explicit high-risk encrypted artifact download path instead of routing it through a safe response-report path.
   - Only increase Browser/Web, Desktop, or Overall completion after repository-visible verification and README truth-sync actually land.

3. **Close the remaining Browser/Web workflow depth gaps that now inform the reserved final 1% / beyond-99 completion discussion.**
   - Full OCR for PDFs.
   - Actual visual redaction and rewritten PDF export instead of blocker/report-only evidence.
   - Richer upload/download and file-picker UX beyond the current bounded helper/download set.
   - Deeper vault/portable workflow UX beyond bounded localhost request/response surfaces.
   - Any auth/session decision work only where a real browser workflow truly needs it.
   - Final browser governance/review polish after the core workflow gaps above are no longer blocking.

4. **Close the remaining Desktop workstation gaps that now inform the reserved final 1% / beyond-99 completion discussion.**
   - Full OCR, actual visual redaction, and rewritten PDF export instead of review/report-only saves.
   - Full desktop execution/file-picker/save UX depth beyond bounded helpers.
   - Deeper vault decode/audit investigation workflows and richer workstation browsing/inspection.
   - Broader portable transfer workflow UX beyond JSON handoff helpers.
   - Remaining DICOM/PDF/media workflow depth that is currently helper-level only.
   - Packaging/hardening and final workstation governance/review polish.

5. **Continue closing the remaining CLI/product-core gaps that still block production readiness.**
   - Conservative media now has CLI PHI-safe metadata-only export evidence via `review-media --export-report` with explicit `rewrite_status: metadata_only_export`; media-byte rewrite/export remains unfinished.
   - Portable transfer UX now includes PHI-safe encrypted artifact inspection plus a `portable-transfer-summary` workflow summary; richer generalized transfer orchestration remains narrower than the final product story.
   - Production packaging/hardening remains unfinished, though local artifact/offline readiness checks provide bounded CLI evidence.
   - Deeper policy/detection coverage is still incomplete, especially where current bounded routes rely on conservative or partial semantics.

6. **Finish the still-missing shared product capabilities that block the repo from claiming end-to-end completion.**
   - Full OCR pipeline.
   - Actual visual redaction.
   - Full PDF rewrite/export.
   - Media-byte rewrite/export beyond metadata-only review.
   - Handwriting handling.
   - FCS semantic parsing.
   - Generalized spreadsheet/workbook handling beyond the current first-non-empty-sheet bounded contract.
   - Cross-surface final verification/audit polish once the real workflow gaps above are closed.

7. **Keep README and cron truth aligned with landed facts only.**
   - README completion numbers must move only when landed, repository-visible work changes the rubric-relevant capability surface.
   - Cron output must explicitly report which of the open action items above advanced, which stayed blocked, what tests ran, and why any completion number did or did not move.
   - Do not replace implementation progress with documentation churn, branch accounting churn, or synthetic action-item splitting.

Superseded historical verification evidence follows for audit trail only. Any completion percentages in the retained historical paragraphs below describe their original point in time and do not override the corrected current snapshot above.

Verification evidence for this truth-sync: cross-surface PDF review page-status artifacts landed on `feat/cross-surface-pdf-page-status-artifacts-cron-1926` in `49551c6` (`feat(browser): add pdf page statuses to reports`) and `5c97faf` (`feat(desktop): add pdf page statuses to reports`). Browser/Web PDF review report downloads and Desktop PDF review report saves now include a PHI-safe `page_statuses` array copied only from primitive/null allowlisted fields (`page`, `status`, `requires_ocr`, `candidate_count`) while excluding raw text, bbox arrays, nested sensitive payloads, PDF bytes/base64 payloads, and unallowlisted runtime response data. Local verification passed: `cargo test -p mdid-browser pdf_review_report_download -- --nocapture`, `cargo test -p mdid-desktop pdf_review_report_save -- --nocapture`, `cargo fmt --check`, and `git diff --check`; SDD spec reviews returned PASS and SDD quality reviews returned APPROVED for both surfaces. At that historical point, the then-current completion text read CLI 95%, browser/web 100%, desktop app 97%, overall 98%; this is superseded by the corrected current snapshot above.

Verification evidence for this truth-sync: cross-surface PDF review report artifacts landed on `feat/cross-surface-pdf-review-artifacts-cron-1900` in `db9ebb6` (`feat(browser): add pdf review report downloads`) and `4fb6c11` (`feat(desktop): add pdf review report saves`). Browser/Web now exposes a dedicated PHI-safe PDF review report JSON download for successful PDF review runtime responses, preserving source-derived filenames while redacting raw text fields, PDF bytes/base64 payloads, bbox/nested structures, and unallowlisted response data. Desktop now exposes a matching workstation PDF review report save helper with Browser-aligned summary/review-queue sanitizer semantics, including `{}`/`[]` defaults for missing containers and skipping non-object queue entries. Local verification passed: `cargo test -p mdid-browser pdf_review_report_download -- --nocapture`, `cargo test -p mdid-desktop pdf_review_report_save -- --nocapture`, `cargo fmt --check`, and `git diff --check`; SDD spec reviews returned PASS after reviewer-driven Desktop sanitizer fixes, and SDD quality reviews returned APPROVED/acceptable for both surfaces. At that historical point, the then-current completion text read CLI 95%, browser/web 100%, desktop app 92%, overall 97%; this is superseded by the corrected current snapshot above.

Verification evidence for this truth-sync: cross-surface safe vault/portable response metadata reports landed on `feat/decode-values-export-parity-cron-1823` in `1ae9a06` (`feat(browser): add safe vault response metadata`), `0550d8c` (`fix(browser): route vault response metadata reports`), `fac48b4` (`fix(browser): preserve runtime response metadata for reports`), `55c649c` (`fix(browser): clear safe metadata on submit`), and `355bd40` (`feat(desktop): add safe vault response metadata`). Browser/Web safe response report downloads now carry only allowlisted primitive/null metadata for successful vault decode, vault audit, vault export, portable inspect, and portable import responses, while preserving separate high-risk decoded-values/audit-events/artifact exports and clearing cached metadata on new submissions. Desktop safe response report saves now carry the same PHI-safe metadata shape for successful decode/audit responses from the last successful runtime response. Local verification passed: `cargo test -p mdid-browser safe_vault_response_report_includes_allowlisted -- --nocapture`, `cargo test -p mdid-browser --lib`, `cargo test -p mdid-desktop safe_response_report_includes_allowlisted -- --nocapture`, `cargo test -p mdid-desktop --lib`, `cargo fmt --check`, and `git diff --check`; SDD spec reviews returned PASS and SDD quality reviews returned APPROVED for both browser and desktop tasks. Completion now reads CLI 95%, browser/web 95%, desktop app 87%, overall 96%.

Verification evidence for this truth-sync: audit-events JSON downloads/saves landed on `feat/audit-event-export-surface-cron-1755` in `996b900` (`feat(browser): add audit events downloads`), `c88e4fe` (`feat(desktop): add audit events saves`), and `c399ec9` (`fix(desktop): preserve audit event metadata shape`). Browser/Web now exposes a separate bounded audit-events JSON download for successful vault audit runtime responses with sanitized source-derived filenames and an allowlisted payload containing `events` plus only present `event_count`, `returned_event_count`, and `next_offset` metadata; it excludes vault path, passphrase, and raw request fields and remains distinct from PHI-safe response reports. Desktop now exposes a matching audit-events JSON save action for successful vault audit responses with source-derived default paths, explicit override preservation, PHI-safe status text, and identical allowlisted metadata shape. Local verification passed: `cargo test -p mdid-browser audit_events_download -- --nocapture`, `cargo test -p mdid-browser --lib`, `cargo test -p mdid-desktop audit_events_export -- --nocapture`, `cargo test -p mdid-desktop --lib`, `cargo test -p mdid-desktop --bin mdid-desktop`, `cargo fmt --check`, and `git diff --check`; SDD spec review returned PASS after the metadata-shape fix and SDD quality reviews returned APPROVED for the browser and desktop tasks. Completion now reads CLI 95%, browser/web 90%, desktop app 82%, overall 95%.

Prior verification evidence retained for context: portable artifact response report downloads/saves landed on `feat/portable-artifact-report-downloads-cron-1726` in `ba830c6` (`feat(browser): add portable response report downloads`), `1ba4d60` (`fix(browser): wire portable response report downloads`), `9e39991` (`fix(browser): redact nested portable report fields`), `40e49b3` (`feat(desktop): add portable response report saves`), `7184a2a` (`fix(desktop): preserve portable report source stems`), and `4be5081` (`fix(desktop): align portable report mime type`). Browser/Web now prepares a separate structured portable artifact response report download for successful JSON portable inspect/import runtime responses while preserving vault export artifact downloads as the explicit high-risk artifact export path, recursively redacts artifact/decoded-values/records/passphrase fields, preserves the existing PHI-safe non-JSON fallback report path, and uses sanitized source-derived filenames. Desktop now exposes a matching portable response report save helper for export/inspect/import responses with recursive redaction, source-derived filenames, PHI-safe status text, and static fallback. Local verification passed: `cargo test -p mdid-browser portable_response_report_download -- --nocapture`, `cargo test -p mdid-browser portable -- --nocapture`, `cargo test -p mdid-browser --lib`, `cargo test -p mdid-desktop portable_response_report_save -- --nocapture`, and `cargo test -p mdid-desktop --lib`; SDD reviews returned PASS/APPROVED for the desktop task and PASS plus reviewer-driven fixes for the browser task. Completion now reads CLI 95%, browser/web 85%, desktop app 77%, overall 94%.

Verification evidence for this truth-sync: desktop portable import file handoff was verified on `feat/desktop-portable-import-file-handoff-cron-1710` in `059db2a`. The desktop helper layer supports loading an allowlisted portable artifact JSON into the workstation portable import request state as well as inspect mode, while rejecting non-artifact-consuming modes such as vault export and preserving PHI-safe redacted debug behavior. Completion now reads CLI 95%, browser/web 80%, desktop app 72%, overall 93%; this round does not change the percentages because the local code already contained the helper/tests before this cron branch recorded and reviewed it. Local and SDD verification for this round passed or was reviewed: `cargo test -p mdid-desktop desktop_portable_file_import_payload -- --nocapture` (2 passed in implementer), `cargo test -p mdid-desktop --lib` (126 passed in implementer), `cargo test -p mdid-desktop portable -- --nocapture` (35 passed in SDD quality review), SDD spec review returned PASS, and SDD quality review returned APPROVED.

Prior verification evidence retained for context: browser vault decode values export landed on `feat/browser-decode-values-export-cron-1617` in `8a0d54c`, `29aa5a1`, `8b0bf6e`, and `86844a2`. The browser vault decode UI now exposes a separate high-risk decoded-values JSON download action only after successful vault decode responses with decoded values, keeps PHI-safe response reports separate, writes decoded values only through the explicit decoded-values export path, uses sanitized source-derived default filenames with static fallback, and clears stale decoded-values state on new submissions, import changes, runtime errors, and import read errors. Completion now reads CLI 95%, browser/web 79%, desktop app 72%, overall 93%. Local verification for this round passed: `cargo test -p mdid-browser decode_values_download -- --nocapture` (3 passed), `cargo test -p mdid-browser vault_decode -- --nocapture` (6 passed), `cargo test -p mdid-browser --lib` (108 passed), `cargo fmt --check`, and `git diff --check`; SDD spec review returned PASS and SDD quality review returned APPROVED for the implementation range.

Prior verification evidence retained for context: desktop vault decode values export landed on `feat/desktop-decode-values-export-cron-1548` in `c88d2e5`, `fd9a276`, `fadbba2`, and `0abe457`. The desktop workstation now exposes a separate high-risk decoded-values JSON save action only after successful vault decode responses with decoded values, keeps PHI-safe response reports separate, writes decoded values only through the explicit decoded-values export path, uses sanitized source-derived default filenames with explicit override preservation, and clears stale save status when a new decode response arrives. Completion now reads CLI 95%, browser/web 78%, desktop app 72%, overall 93%. Local verification for this round passed: `cargo test -p mdid-desktop decode_values_export -- --nocapture` (6 passed), `cargo test -p mdid-desktop app_save_decode_values -- --nocapture` (1 passed), `cargo test -p mdid-desktop decode_values_save_path -- --nocapture` (4 passed), `cargo test -p mdid-desktop --lib` (126 passed), `cargo test -p mdid-desktop --bin mdid-desktop` (34 passed), and SDD spec/quality reviews returned PASS/APPROVED for the helper and UI tasks.

Prior verification evidence retained for context: desktop vault audit pagination controls landed in `d584024` (`feat(desktop): add vault audit offset requests`) and `26e1a9e` (`feat(desktop): show vault audit pagination status`) on `feat/desktop-vault-audit-pagination-controls`. RED evidence was observed for missing `audit_offset` request support and then for missing vault audit pagination status in the desktop summary. Local verification passed: `cargo test -p mdid-desktop vault_audit -- --nocapture` (10 passed), `cargo test -p mdid-desktop --lib` (120 passed), `cargo fmt --check`, and `git diff --check`. SDD spec review returned PASS and SDD quality review returned APPROVED for both implementation tasks. Completion percentages read at that point: CLI 95%, browser/web 78%, desktop app 71%, overall 93%.

Prior verification evidence retained for context: browser vault audit pagination status landed in `64248c9` (`feat(browser): show vault audit pagination status`) and was hardened in `d1d8c44` (`fix(browser): use audit output for pagination status`) on `feat/browser-vault-audit-pagination-status`. RED evidence was observed for the missing helper, then quality review found and fixed the `event_count` versus `returned_event_count` page-window bug plus the real runtime-output integration gap and valid-JSON-summary fallback gap. Local verification passed: `cargo test -p mdid-browser vault_audit_pagination_status -- --nocapture` (7 passed), `cargo test -p mdid-browser --lib` (103 passed), `cargo fmt --check`, and `git diff --check`. SDD spec review returned PASS and SDD quality review returned APPROVED. At that prior point, completion percentages read: CLI 95%, browser/web 78%, desktop app 70%, overall 93%.

Verification evidence for this truth-sync: CLI portable blank context validation landed in `bdd116c` (`fix(cli): reject blank portable context`) on `fix/cli-portable-blank-context-validation`. RED evidence was observed by adding `parse_vault_export_rejects_blank_context_without_echoing_value` and `parse_vault_import_rejects_blank_context_without_echoing_value` before implementation; after adding the minimal parser checks, `cargo test -p mdid-cli blank_context -- --nocapture`, `cargo test -p mdid-cli vault_export -- --nocapture`, `cargo test -p mdid-cli vault_import -- --nocapture`, `cargo fmt --check`, and `git diff --check` all passed. SDD spec review returned PASS and SDD quality review returned APPROVED for the landed commit. Completion percentages are unchanged: CLI 95%, browser/web 76%, desktop app 70%, overall 93%.

Verification evidence for this truth-sync: vault audit pagination contract hardening landed in `1c04471` (`feat: add vault audit pagination`) on `fix/vault-audit-pagination`. Local verification passed: `cargo test -p mdid-runtime --test runtime_http vault_audit -- --nocapture`, `cargo test -p mdid-cli --bin mdid-cli vault_audit -- --nocapture`, `cargo test -p mdid-cli --test cli_smoke vault_audit -- --nocapture` (0 selected / 15 filtered, no matching smoke tests), `cargo test -p mdid-browser --lib vault_audit -- --nocapture`, `cargo fmt --check`, and `git diff --check`. SDD spec review returned PASS and SDD quality review returned APPROVED. Completion percentages now read: CLI 95%, browser/web 77%, desktop app 70%, overall 93%.

Prior verification evidence retained for context: CLI duplicate artifact verification guard landed in `6c299ae` (`fix(cli): reject duplicate artifact verification paths`). Local verification for that guard: `cargo test -p mdid-cli verify_artifacts -- --nocapture`, `cargo test -p mdid-cli --test cli_smoke cli_verify_artifacts_reports_missing_and_exits_nonzero -- --nocapture`, `cargo fmt --check`, and `git diff --check` all passed. Completion percentages were unchanged: CLI 95%, browser/web 76%, desktop app 70%, overall 93%.

Prior verification evidence retained for context: desktop workflow output source-name save suggestions landed in `6f61713` (`fix(desktop): use source names for workflow output saves`) on `fix/desktop-workflow-output-source-filenames` and passed SDD spec review (PASS) plus SDD quality review (APPROVED). Local verification for that round: `cargo test -p mdid-desktop workflow_output_save_path_uses_import_source_stem_after_dicom_success -- --nocapture`, `cargo test -p mdid-desktop workflow_output_save_path -- --nocapture`, `cargo test -p mdid-desktop --lib`, `cargo fmt --check`, and `git diff --check` all passed. Completion percentages were unchanged: CLI 95%, browser/web 76%, desktop app 70%, overall 93%.

Prior verification evidence retained for context: local verification re-ran the CLI scope-quarantine regression, `cargo test -p mdid-browser --lib portable_artifact -- --nocapture`, `cargo test -p mdid-desktop source_name_for_save_suggestions -- --nocapture`, `cargo fmt --check`, and `git diff --check` on `develop`; SDD promotion spec review returned PASS and SDD promotion quality review returned APPROVED for the `main..develop` promotion candidate. A local `main` promotion was attempted but blocked before merge because `main` is currently checked out by the existing `/tmp/med-de-id-mergecheck` worktree, which is outside the allowed product workspaces for that cron run; no main files were modified. The previous desktop tabular import source-name slice landed in `ac7b946` (`fix(desktop): preserve tabular import source names`) and was verified with `cargo test -p mdid-desktop source_name_for_save_suggestions -- --nocapture`, `cargo test -p mdid-desktop --lib`, `cargo fmt --check`, `git diff --check`, SDD spec review PASS, and SDD quality review APPROVED. Desktop app increased from 69% to 70% in that earlier slice; overall remains 93% because these rounds remove bounded helper/promotion gaps but do not remove larger >=95% blockers such as full desktop file picker/save UX depth, richer workflows, OCR/visual redaction, PDF/media rewrite/export, full vault browsing/execution UX, packaging/hardening, or deeper policy/detection.

Prior verification evidence retained for context: the browser portable import handoff slice landed in `fe7e001` (`fix(browser): preserve portable import mode on artifact drop`) and was verified with `cargo test -p mdid-browser portable_artifact -- --nocapture`, `cargo test -p mdid-browser --lib`, `cargo fmt --check`, and `git diff --check`. Browser/web increased from 75% to 76%; overall remained 93% because this removed a bounded browser upload UX gap but did not remove larger >=95% blockers such as richer workflows, OCR/visual redaction, PDF/media rewrite/export, full vault browsing/execution UX, packaging/hardening, or deeper policy/detection.

Prior verification evidence retained for context: the browser vault export source-aware artifact filename slice landed on `develop` in `086f9a2` (`feat(browser): use vault source name for export artifacts`), `d9cd3b2` (`fix(browser): fall back for unsafe vault export names`), and `07cb302` (`fix(browser): keep default vault export filename fallback`) and was verified with `cargo test -p mdid-browser --lib browser_vault_export_download -- --nocapture`, `cargo test -p mdid-browser --lib`, and `git diff --check`.

Prior verification evidence retained for context: the browser PDF source-name fallback landed in `695432d` (`feat(browser): use PDF source name for report downloads`) and was verified with `cargo test -p mdid-browser --lib pdf_review_download_uses_safe_source_name_when_no_imported_file_exists -- --nocapture` and `cargo test -p mdid-browser --lib`. Earlier repository truth-sync evidence on `develop` included `cargo test -p mdid-browser --lib browser_vault_response_downloads_use_safe_source_filenames -- --nocapture`, `cargo test -p mdid-browser --lib`, `cargo test -p mdid-desktop response_report -- --nocapture`, `cargo test -p mdid-desktop --lib`, the CLI scope-quarantine regression, and `git diff --check`. Desktop vault/portable source-aware report filename evidence included `cargo test -p mdid-desktop`, `cargo test -p mdid-desktop response_report -- --nocapture`, `cargo clippy -p mdid-desktop --all-targets -- -D warnings`, `cargo fmt --check`, and final SDD spec review PASS plus quality review APPROVED for the landed range `679272c..3a2909a`.

Missing items still block broader completion: deeper detection/policy, richer browser/desktop workflows beyond the newly landed bounded PDF review artifacts, full OCR, visual redaction, deeper and fuller vault UX beyond bounded audit/decode/export/import request surfaces, full desktop vault/decode/audit execution and audit-investigation workflow UX, full portable transfer workflow UX beyond bounded localhost request/response surfaces and desktop artifact JSON handoff, remaining file picker/upload-download depth beyond bounded import-aware export naming, governance/review polish, media-byte rewrite/export, full PDF rewrite/export, packaging/hardening, deeper policy/detection crates, full review/governance workflows, richer browser UX and upload/download depth beyond the bounded CSV/XLSX/PDF/DICOM import/export helpers, metadata-only JSON media review, deeper desktop vault browsing, desktop decode workflow execution UX, desktop PDF flow beyond request preparation and bounded review/export helper naming, desktop DICOM flow beyond bounded request/response/import/export helper support, broader import/export and upload flows, handwriting handling, FCS semantic parsing, generalized spreadsheet handling, auth/session handling where needed, and final verification/audit polish across surfaces. Remaining CLI gaps include conservative-media rewrite/export, generalized transfer workflow UX, richer workflows, and production packaging/hardening.

This repository currently contains the Slice 1 workspace foundation, the Slice 2 vault MVP, the first Slice 3 tabular workflow and adapter work, the bounded Slice 5/6 PDF support foundation, and bounded runtime HTTP entries for DICOM de-identification, tabular CSV/XLSX de-identification, PDF review, conservative media metadata review, vault decode, bounded vault audit browsing, bounded portable subset export, bounded portable artifact inspection, and bounded portable artifact import into a local vault.

Implemented so far:

- Shared domain models for pipeline, review, vault mapping, decode requests, audit events, and tabular workflow state
- Conservative media/FCS domain workflow models, bounded adapter foundation, and application-layer review routing now distinguish image/video/FCS metadata-only status, OCR-or-visual-review-required status, unsupported payloads, review-required metadata candidates, honest summary counts, and redacted candidate/reference/output debug; browser and desktop can prepare or submit bounded metadata-only conservative media JSON review requests against the landed local runtime surface, but this foundation still does not implement OCR, visual redaction, FCS semantic parsing, media rewrite/export, or generalized browser/desktop media workflows
- An encrypted `mdid-vault` crate with local file-backed storage, explicit decode-by-record-id, audit recording, portable subset export, bounded portable artifact import, deterministic duplicate/normalization handling via the shared import contract, and repeated-value token reuse
- An implemented `mdid-adapters` crate with shared tabular extraction for CSV/XLSX inputs, schema inference, field-level PHI candidate policies, and blank-cell handling parity
- CSV/tabular import hardening strips a leading UTF-8 BOM from the first header before policy matching, so BOM-prefixed CSV exports still match explicit field policies; this is a narrow adapter normalization and does not broaden upload/import workflows
- Tabular application behavior that composes the adapters with vault-backed reversible encoding and honest batch summaries
- Bounded PDF support for text-layer extraction, OCR-needed suspicion routing, mixed multi-page summary/reporting, and invalid-PDF rejection as parse failure
- Current PDF support does not yet perform full OCR, visual redaction, handwriting handling, or final PDF rewrite/export
- `mdid-runtime` now exposes a bounded local HTTP DICOM de-identification entry that accepts local/base64-transported DICOM bytes, applies the existing private-tag policy service logic, returns rewritten DICOM bytes plus a review summary/review queue, and honestly rejects invalid DICOM payloads
- `mdid-runtime` also exposes bounded local HTTP tabular de-identification entries: one accepts CSV text plus explicit field policies and returns rewritten CSV plus a summary and review queue; another accepts base64-transported XLSX workbook bytes plus explicit field policies and returns rewritten workbook bytes plus a summary and review queue, but only extracts and rewrites the first non-empty worksheet and does not offer caller-controlled sheet selection. These entries still do not imply multipart upload flows, generalized spreadsheet browsing/import/export APIs, workbook-wide fidelity guarantees, or any auth/session handling
- `mdid-runtime` also exposes a bounded local HTTP `/pdf/deidentify` review entry that accepts base64 PDF bytes plus a source name, delegates to the existing PDF application service, reports the existing text-layer/OCR-required summary, page statuses, and review queue, and returns `rewritten_pdf_bytes_base64: null`; it does not perform OCR, handwriting handling, visual redaction, PDF rewrite/export, desktop PDF flow, auth/session handling, generalized upload workflows, or broader workflow behavior
- `mdid-runtime` exposes a bounded local HTTP conservative media review entry accepting image/video/FCS metadata JSON, routes through the application review service, returns a summary/review queue and `rewritten_media_bytes_base64: null`; it explicitly does not implement OCR, visual redaction, FCS semantic parsing, media rewrite/export, multipart upload, browser/desktop flows, auth/session, or generalized media workflow behavior
- `mdid-runtime` also exposes a bounded local HTTP vault decode entry that unlocks a local vault with an explicit passphrase, decodes only the requested record scope, returns decoded values plus the resulting audit event, and honestly rejects wrong passphrases, unknown records, invalid decode requests, and unusable vault targets
- `mdid-runtime` also exposes a bounded local HTTP vault audit browsing entry that unlocks a local vault with an explicit passphrase, returns persisted audit events in reverse chronological order with bounded filtering, supports filtering by event kind and actor, and remains read-only
- `mdid-runtime` also exposes a bounded local HTTP portable export entry that unlocks a local vault with an explicit passphrase, exports only the requested bounded record subset into an encrypted portable artifact, records the resulting export audit event, and remains scoped to local export creation rather than import or transfer workflows
- `mdid-runtime` also exposes a bounded local HTTP portable artifact inspection entry that locally unlocks an encrypted portable artifact with an explicit portable passphrase and returns a bounded preview of persisted record fields from the encrypted artifact contents, including sensitive persisted values already stored in the artifact such as tokens and original values
- `mdid-runtime` also exposes a bounded local HTTP portable artifact import entry that unlocks a local vault with an explicit vault passphrase, imports an encrypted portable artifact into that local vault, skips duplicate record ids and existing semantic duplicates while deterministically normalizing shared-value token reuse through the shared import contract, records the resulting import audit event, and returns bounded imported/duplicate counts rather than artifact contents or generalized transfer state
- `mdid-browser` is no longer only a scaffold: it provides bounded local-first browser modes for tabular CSV/XLSX de-identification, PDF review-only reports, DICOM tag-level helper output, conservative media metadata review, vault audit/decode, vault export, portable artifact inspect/import, Privacy Filter summary downloads, OCR handoff summary downloads, OCR-to-Privacy-Filter summary downloads for existing CLI/runtime reports, and bounded localhost request surfaces; they do not provide DICOM pixel redaction, OCR execution, visual redaction, handwriting handling, rewritten PDF export, full vault browsing, raw decoded-value browsing, generalized portable transfer workflow UX, auth/session handling, or broader workflow-platform behavior.
- `mdid-browser` still does not provide DICOM pixel redaction, OCR, visual redaction, handwriting handling, PDF rewrite/export, full vault browsing, raw decoded value rendering, generalized portable transfer workflow UX beyond bounded localhost request/response surfaces, richer browser upload/download UX depth, desktop PDF flow, auth/session handling, generalized workflow behavior, a generalized workflow builder, or any broader browser governance workspace
- `mdid-desktop` now renders a bounded sensitive-workstation foundation for preparing local runtime CSV, XLSX, PDF review, DICOM, metadata-only conservative media JSON review, vault decode/audit, and portable artifact export/inspect/import requests with endpoint previews, validation status, mode-specific disclosures, bounded CSV/XLSX/PDF/DICOM file import/export helpers, localhost runtime submission, and local runtime-shaped summary, review queue, rewritten-output/review-notice, and error panes, including PHI-safe rendering for media metadata review responses and PHI-safe structured JSON export of already-rendered bounded PDF review reports, media metadata review reports, vault/portable response summaries/notices/errors, and OCR-to-Privacy-Filter summaries for existing CLI/runtime reports. It still does not implement deeper desktop vault browsing, decoded-value display, audit investigation workflow, generalized portable transfer workflow UX, OCR, visual redaction, media-byte upload/rewrite/export, PDF rewrite/export, desktop DICOM flow beyond bounded request/response/import/export helper support, full DICOM review workflow, or full review workflows
- `mdid-cli` remains a de-identification automation surface and includes bounded CSV/XLSX/DICOM de-identification, PDF review report generation, conservative media metadata review, vault audit/decode/export/import/inspect, artifact verification, Privacy Filter text/corpus runners, PP-OCRv5 mobile small OCR handoff wrappers, OCR handoff corpus evidence, OCR-to-Privacy-Filter corpus and single-image bridge wrappers, and aggregate PHI-safe summary artifacts. CLI reports are scoped to local files and bounded JSON contracts; PDF output remains review-only, OCR/Privacy Filter evidence remains CLI/runtime POC evidence, and none of these commands claim visual redaction, image pixel redaction, handwriting recognition, final PDF rewrite/export, Browser/Web execution, Desktop execution, or model-quality proof.

The current runtime HTTP slice is intentionally narrow: it is bounded to local request bodies for DICOM, CSV/tabular, XLSX workbook bytes, PDF review, conservative media metadata review, vault decode/audit/export, portable artifact inspection/import, Privacy Filter summary/text requests, OCR handoff summary requests, and OCR-to-Privacy-Filter summary requests. These runtime routes return bounded summaries, review queues, rewritten tabular/DICOM bytes where explicitly supported, or PHI-safe aggregate summaries; they do not perform OCR model execution, visual redaction, handwriting recognition, media rewrite/export, final PDF rewrite/export, Browser/Web execution, Desktop execution, auth/session handling, or generalized workflow-platform behavior.

Planned next from the design:

- Additional policy and detection crates
- Deeper application behavior and de-identification surface behavior beyond the current scaffolds

Available docs:

- Design spec: `docs/superpowers/specs/2026-04-25-med-de-id-design.md`
- Initial implementation plan: `docs/superpowers/plans/2026-04-25-med-de-id-foundation-implementation-plan.md`
- Slice 2 vault/decode MVP plan: `docs/superpowers/plans/2026-04-25-med-de-id-slice-2-vault-encode-decode-mvp.md`
- Slice 3 tabular deep-support plan: `docs/superpowers/plans/2026-04-25-med-de-id-slice-3-tabular-deep-support.md`
- Desktop conservative media review plan: `docs/superpowers/plans/2026-04-29-desktop-conservative-media-review.md`
- Desktop vault audit limit plan: `docs/superpowers/plans/2026-04-29-desktop-vault-audit-limit.md`
- Desktop vault safe response download plan: `docs/superpowers/plans/2026-04-30-desktop-vault-safe-response-download.md`
- Portable artifact response downloads/saves plan: `docs/superpowers/plans/2026-04-30-portable-artifact-response-downloads.md`

## Roadmap

- **v1**: governed workflow core, vault/decode controls, audit trail, tri-surface skeleton, deep CSV support, bounded XLSX runtime support, DICOM tag-level support, bounded PDF/scanned-record foundation, conservative image/video/FCS support
- **v1.5**: detection quality/provenance upgrades, PDF/DICOM policy depth, parity and workflow polish
- **v2**: AI/NLP detectors, stronger media handling, richer custom node/plugin model, enterprise controls

## Repo conventions

- Planning and design docs live under `docs/superpowers/`
- Implementation is expected to follow TDD and small verified slices
- The browser tool is local-first and served on `127.0.0.1`, not a SaaS deployment

## License

Workspace metadata is currently marked `UNLICENSED`.
